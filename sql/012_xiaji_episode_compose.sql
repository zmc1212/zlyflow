-- 导台2 剧集成片：状态与导出字段。列由 XiajiEpisodeStore.initialize 的 ensure_column 补齐。
-- 文档基线字段：
-- xiaji_episodes.compose_status / compose_url / compose_key / compose_error
-- compose_resolution / compose_add_subtitles / compose_duration_sec / compose_at / compose_progress
-- xiaji_beats.audio_url（解说剧合成门闩；本版不入队 TTS）
SELECT 1;
