# ZLY AI Video Studio 项目规则

## AI 开始工作前

1. 先阅读本文件、`README.md`、`功能说明与扩展指南.md`，以及 `docs/ARCHITECTURE.md`；涉及导演台的工作还需阅读 `docs/导演台流程与界面结构.md`（流程与界面快照，含参考截图索引）。
2. 以当前代码和架构快照为准，不要依据旧对话推断接口、节点 ID 或目录。
3. 修改前先检查工作区状态，保留用户已有改动，不回滚无关文件。

## 运行边界

- 工作台前端由 `frontend` 构建，FastAPI 后端位于 `backend/app`。
- 工作台后端地址固定为 `http://127.0.0.1:7865`；本地开发入口是 Vite 开发服务器 `http://127.0.0.1:5173`（`启动本地视频工作台.bat` 自动打开，`/api` 代理到 7865，前端修改热更新）。修改前端后一律在 5173 页面自查，不要在 7865 静态页验证开发中的改动。
- `frontend/dist` 是 FastAPI 生产静态产物：只在部署（Docker 镜像 / 服务器）或需要 7865 静态入口（局域网访问、验证构建产物）时才执行 `pnpm --dir frontend build`；日常本地开发不要为看效果反复打包。构建必须走 `pnpm build`（内含 `clean-dist.mjs` 清空 dist；本机 Node 的 `fs.rmSync` 会静默失败，直接 `vite build` 会残留历史产物）。
- 在 7865 静态入口验证时，构建完成后必须刷新浏览器再检查：标签页只在加载时拉取一次 JS，不刷新会一直运行旧 bundle，会造成“改动未生效”的误判。
- ComfyUI 默认实例为 `http://127.0.0.1:8188`，可通过管理设置或 `ZLY_AI_VIDEO_STUDIO_COMFY_URL` 配置；工作台同一时间只连接一个实例。
- ComfyUI 根目录固定为工作台目录父级下的 `整合包及模型\comfyui-integrate-v1.3\comfyui-integrate\Comfyui`。
- 禁止为工作台另部署第二套 ComfyUI。
- VACE 自定义节点只能位于固定 ComfyUI 的既有 VACE 多参考图节点目录，不得重命名或迁移。

## 当前核心架构

- 前端通过 `/api/modes` 获取工作流注册表，不硬编码模型选择列表。
- 后端 `workflow_registry.py` 是工作流能力、参考图数量和参数校验的唯一来源。
- MiniMax H3 使用 `minimax_h3_workflow.py` 动态生成 ComfyUI API graph。
- H3 支持 T2V（0 张）、I2V（首帧必填、尾帧可选）和 R2V（1-9 张有序参考图）。
- R2V 提示词使用 `<Picture n>` 与上传顺序对应，前端负责插入和排序。
- 任务通过远程 MySQL（`docs/存储配置.md` 的 `ai-media`）记录；媒体走七牛云（管理设置中的 Bucket/域名/AKSK）。unittest 使用临时 SQLite。

## 工作流参数产品化标准

- `workflow_registry.py` 必须保存工作流的完整参数、默认值和校验约束；前端不得按参数名硬编码显示规则。
- 每个 `options` 参数必须声明 `ui_group`：`primary`、`advanced` 或 `internal`。未明确判断时使用 `internal`，禁止默认向用户暴露。
- `primary` 只放普通用户能理解、会直接影响创作结果的必要选项，例如画面比例、时长和输出数量；同一工作流原则上不超过 4 项。
- `advanced` 只放创作者能理解且确有调整价值的选项，例如画质、随机种子、创意强度；同一工作流原则上不超过 4 项，并使用“更多设置”渐进展示。
- `internal` 包含任务类型自动路由、节点选择、采样步数、Shift、模型/CLIP/VAE/LoRA、设备/精度、显存和编码参数。创建表单不得显示这些参数，由后端默认值托管。
- 技术参数如确需开放，必须先包装成用户可理解的语义预设，再映射到内部参数；不得直接把 ComfyUI 节点控件照搬到工作台。
- 任务详情默认只显示创作参数；完整有效参数快照必须保留，并在折叠的“运行参数”中回显，以支持复现和排障。
- 新接入工作流的测试必须校验参数层级、默认值、后端 graph 映射和任务回显；涉及界面时必须验证默认层级、展开交互和移动端布局。

## 前端组件库标准

- 工作台统一使用开源 `Ant Design` 组件库（`frontend` 中的 `antd`）提供业务表单、选择、数字输入、开关、弹层和反馈组件。
- 禁止为已有 Ant Design 能力手写或复制 Select、Input、InputNumber、Switch、Modal、Dropdown、Popover、Tooltip、Tabs、Table 等业务组件；需要新能力时先查阅并直接使用组件库。
- 只允许以布局容器、主题 token 和项目样式类适配工作台视觉，不得封装一层同功能的本地 UI 组件。文件上传等浏览器安全 API 必需控件可保留原生入口。
- 新增 UI 依赖必须选择维护活跃、许可清晰的开源组件库，并在 `frontend/package.json` 锁定；升级时需完成前端构建和桌面、移动端回归检查。

## 双主题高对比度视觉标准

- 创作工作台、登录流程、导演台与管理员设置统一支持浅色和暗色主题；浅色为默认值，用户选择按浏览器本地持久化。主题必须由根级 Provider、Ant Design token 与 `data-theme` 语义样式共同驱动，禁止各页面维护独立主题状态。
- 浅色基线为画布 `#f8f9fa`、表面 `#ffffff`、主文本 `#171a1f`；暗色基线为画布 `#0f1115`、表面 `#171a20`、抬升表面 `#1e2229`、主文本 `#f3f5f7`。媒体预览可保持近黑色，其余区域必须用表面层级和边框区分，避免无层次纯黑。
- 新增界面颜色优先使用 `--studio-*` 语义变量或 Ant Design token，禁止在同一主题中混入另一主题的硬编码底色与文本色。Alert、Message、Tooltip、Dropdown、Select、Modal 等传送到 `body` 的弹层也必须继承当前主题。
- 所有表单 Label、说明文本、表格表头、数据内容和状态标记都必须在两种主题下保持清晰对比；状态颜色不得单独承载含义，并应同时提供文字或图标。


## 重大架构变更记录规则

涉及目录、端口、API、数据库、工作流协议、ComfyUI 节点、模型路径、任务队列或前端状态模型的变更，必须同时更新：

1. `docs/ARCHITECTURE.md`：更新当前基线和迁移说明。
2. `功能说明与扩展指南.md`：在末尾增加带日期的变更章节。
3. `README.md`：同步启动方式或用户可见行为变化。

每条记录至少写明日期、变更原因、受影响文件、兼容性、验证命令和回滚方式。完成代码后至少运行后端测试和前端构建；涉及界面时补充桌面与移动端检查。

## 导演台 AI 对话界面视觉标准（严格遵守）

- 导演台对话创作室（剧本阶段生成视图、Prompt Bar、澄清问题卡、任务面板、流式文本等）的视觉样式**必须逐像素对照官方源码复刻**：`https://github.com/TurboKach/ai-native-react-components`（beautifului.dev 同源，MIT）。新增或修改该区域任何组件样式前，必须先拉取对应组件的 `.tsx` 源码与 `app/globals.css` token 块对照，不得凭印象手写。
- 复刻方式：只移植**设计 token、尺寸、圆角、阴影、间距、动效曲线与关键帧**到本项目的 `--director-*` 变量层与样式类；不安装 shadcn/Tailwind v4 依赖，不整文件复制组件（官方组件是自驱动 demo，需按本工作台真实状态接线，且遵守 Ant Design 统一规范）。
- 双主题：浅色/暗色都必须使用官方 `globals.css` 中对应主题的 token 值成对配置。
- 每次修改后必须用浏览器打开真实工作台页面自查（截图对比），确认与官方 demo 视觉一致后才算完成；禁止只跑构建不验视觉。
- 对话创作室之外的页面（生成页、导台2、管理后台等）不受本条约束，继续使用全局 `--studio-*` 主题。

## 导演台流程文档维护规则

- `docs/导演台流程与界面结构.md` 是导演台（首页 / 导演创作九阶段 / 剪辑 / 短视频批量 / 参考片复刻）流程与界面结构的权威快照，包含路由地图、各阶段 UI 结构与真实文案、`--director-*` 设计 token、关键文件映射和参考截图索引（`docs/design-ref-director/`）。
- 凡新增或修改导演台的路由、阶段流程、页面布局、组件、用户可见文案或设计 token，必须在同一次改动中更新该文档对应章节；界面变化可见时按文档附录的步骤重新生成参考截图并更新基线日期。
- 为导演台生成设计稿或 AI 生图时，提示词必须以该文档 + 参考截图为准，不得凭印象描述页面。

## 测试与临时文件规范（严格遵守）

- **禁止污染根目录与业务源码目录**：严禁在项目根目录、前端 `frontend/` 源码目录或后端 `backend/` 业务目录下直接生成临时测试脚本、临时排查脚本、测试截图或过程产物（例如 `.tmp-*`、`test_*.js`、`*.tmp.png` 等）。
- **测试代码规范归档**：
  - 单元测试与集成测试代码必须放置在规范的测试目录下（前端使用 `frontend/src/**/*.test.ts(x)`，后端使用 `backend/tests/`）。
  - 禁止在源码目录或根目录中随意遗留即席执行的单次脚本（如 `test_parse.cjs`、`generate_test.js`）。如需临时调试，应通过框架测试用例运行，或在验证完毕后立即清理，绝不可遗留。
- **临时测试与排障产物规范**：
  - UI 视觉排查、Playwright 截图或自动化测试过程中产生的图片和日志，必须统一输出到项目测试输出目录（如 `test-results/` 或专用临时目录 `tmp/` / `.tmp/`），绝不能直接写在项目根路径。
  - 排查与验证结束后，应主动清理临时测试文件，确保工作区干净。
- **版本控制忽略**：所有测试输出目录（`test-results/`、`playwright-report/`）及临时文件模式（`tmp/`、`.tmp/`、`*.tmp*` 等）必须严格在 `.gitignore` 中声明并保持忽略，严禁提交到 Git。

## 禁止事项

- 不要直接修改已接入工作流的节点 ID；需要变化时复制为新工作流并建立新映射。
- 不要把 API 格式工作流和普通前端工作流 JSON 混用。
- 不要下载模型到工作台目录或临时目录；模型路径必须通过 ComfyUI 目录或 `extra_model_paths.yaml` 管理。
- 不要在未记录架构变更的情况下进行跨模块重构。
- 严禁在项目根目录或业务源码目录生成临时测试文件、调试日志或排障截图；测试产物必须保存在已加入 `.gitignore` 的测试目录中，并在验证后及时清理。
