# 本地视频工作台：功能说明与扩展指南

更新日期：2026-08-11

## WebUI 1.0 架构说明

页面为独立的 React + TypeScript 创作工作台，后端为 FastAPI；ComfyUI 工作流和节点 ID 保持不变。生产模式由 FastAPI 在 `127.0.0.1:7865` 同时提供前端静态文件与 API，完成视频由浏览器从受保护的工作台下载地址流式写入员工授权目录，不在服务器临时暂存。

```text
React 创作工作台
  -> FastAPI 会话/RBAC -> /api/jobs 串行任务队列 + SQLite
    -> ComfyUI /upload/image、/prompt、/history
      -> 受保护下载 URL -> ComfyUI /view 流式转发 -> 浏览器授权目录 -> 员工电脑 -> delivered 回执
```

- `frontend`：Vite、React、TypeScript、Tailwind 和 shadcn/ui 风格组件；包含创作工作台、任务记录、作品库。
- `backend/app`：上传、ComfyUI API 客户端、SQLite 记录、单任务 worker 和 FastAPI 路由。
- `data/zly-ai-video-studio.db`：用户、会话、审计、任务与输出元数据；`data/uploads`：按用户划分的上传素材；`data/staging`：仅保留旧任务与多阶段工作流的短暂兼容文件，不存放新 H3 成片。
- `启动本地视频工作台.bat`：唯一启动入口，负责启动 React + FastAPI 工作台。
- 默认 ComfyUI 地址为 `http://127.0.0.1:8188`。超级管理员可在「管理设置 → AI 供应商」修改并测试连接；也可通过 `ZLY_AI_VIDEO_STUDIO_COMFY_URL` 作为首次启动默认值。工作台同一时间只连接一个 ComfyUI 实例。新工作流仍必须保存为 API 格式 JSON，且不要改动已映射工作流的节点 ID。

### Wan VACE 三图参考节点：固定位置与启动规则

`WanVaceMultiReference` 是工作台 VACE 三图参考模式的必要自定义节点。它的唯一安装位置固定为：

```text
D:\zlyun\ZLY AI Video Studio\整合包及模型\comfyui-integrate-v1.3\comfyui-integrate\Comfyui\custom_nodes\zly-ai-video-studio_vace_multi_reference
```

- 只使用上述 `Comfyui` 目录启动的主 ComfyUI 服务（`http://127.0.0.1:8188`）。不得为节点单独启动其他 ComfyUI 实例，也不得改用 `8189` 等其他端口。
- 对节点的安装、迁移或更新只能发生在上述 `custom_nodes` 目录；禁止复制或下载到 `本地视频工作台`、其他整合包、临时目录或独立 Python 环境。
- 修改节点后，重启主 ComfyUI 服务一次；打开 `http://127.0.0.1:8188/object_info`，确认其中含有 `WanVaceMultiReference`，再启动工作台。
- 工作流节点 `10`、`11` 将场景、主体、风格三张图组成 `IMAGE` 批次；节点 `12` 按 Wan VACE 官方 R2V 语义独立编码每张参考图，并沿参考时序拼接 latent。不要把三图横向拼接成单张 contact sheet。
- VACE 1.3B R2V 采样参数遵循官方 `vace_wan_inference.py` 默认值：`ModelSamplingSD3 shift=16`、`KSampler steps=50`、`cfg=5`、`uni_pc/simple`。

### Wan UMT5 文本编码器兼容性

主整合包内的 `models/text_encoders/umt5_xxl_enc_bf16.pth` 使用 ComfyUI 原生权重键名（`token_embedding.weight`、`blocks.*`）。已在固定 ComfyUI 目录的 `comfy/sd.py` 补充识别规则，使其按 Wan UMT5-XXL 而非普通 CLIP-L 加载。不要替换、重命名、重复下载或移动该模型文件。

该单文件权重未内嵌 SentencePiece tokenizer。唯一补充文件固定为：

```text
D:\zlyun\ZLY AI Video Studio\整合包及模型\comfyui-integrate-v1.3\comfyui-integrate\Comfyui\comfy\text_encoders\umt5_tokenizer\spiece.model
```

- 文件来源：Google 发布的 [`google/umt5-xxl/spiece.model`](https://huggingface.co/google/umt5-xxl/blob/main/spiece.model)；已校验 SHA-256：`e3909a67b780650b35cf529ac782ad2b6b26e6d1f849d3fbb6a872905f452458`。
- 不得把它下载到工作台、临时目录、模型根目录之外或其他 ComfyUI；无需、也不得再下载 11 GB 的 `umt5_xxl_enc_bf16.pth`。
- `comfy/text_encoders/wan.py` 在权重未内嵌 tokenizer 时只读取上述固定文件；`spiece_tokenizer.py` 已针对中文安装路径改为“文件路径失败时读取内存字节”，避免 Windows SentencePiece 的非 ASCII 路径错误。
- 此整合包启用 Windows AIMDO 低内存加载时，会将标准 `[out_features, in_features]` 的 UMT5 前馈层临时映射为反向形状。`comfy/text_encoders/wan.py` 中的 `WanNativeOps` 仅在 Wan UMT5 的非方阵线性层运行时恢复正确方向；它不修改 `umt5_xxl_enc_bf16.pth`，也不影响其他模型。
- 修改这些兼容文件或更新主 ComfyUI 后，必须重启固定目录的主 ComfyUI（8188）；不要启动 8189 或任何额外服务。

## 1. 当前用途

本目录提供一个面向同事使用的轻量本地网页。用户不需要进入 ComfyUI 节点界面，只需填写提示词、按需上传参考图，即可调用本机 ComfyUI 的本地模型生成图片、首帧和视频。

当前链路：

```text
图片提示词 + （可选）一张参考图
        -> Flux2-Klein 本地文生图 / 参考图生图
        -> PNG 文件

三张参考图 + 首帧提示词
        -> Flux2-Klein 本地首帧生成
        -> 首帧图片 + 视频运动提示词
        -> LTX 2.3 本地图生视频
        -> MP4 文件
```

工作流只调用本机模型，不会调用即梦、可灵、ChatGPT Images 或其他按点数计费的在线生图/生视频接口。实际成本为显卡、电力、维护和存储。

## 2. 已实现功能

1. 文生图：输入图片提示词即可使用 Flux2-Klein 生成图片，提供横版、方图、竖版三种画面尺寸。
2. 可选参考图：上传一张参考图时，工作流会将其编码为 Flux2 的正、负参考条件，辅助保留主体、服装、风格或场景特征。
3. 三张参考图上传：分别传入 Flux2-Klein 三个参考图节点。
4. 高质量首帧：填写“首帧提示词”后，可单独生成并预览首帧。
5. 首帧串联视频：点击“生成首帧并制作视频”后，首帧会自动作为 LTX 2.3 的输入图，不需要人工下载再上传。
6. 视频运动提示词：可单独描述镜头运动、人物动作、环境动态和稳定性约束。
7. 自动随机种子：每次任务都替换种子，避免 ComfyUI 将相同请求当作缓存任务。
8. 结果回传：生成的图片、首帧和 MP4 会显示在网页，并写入 `results` 目录。
9. 单任务排队：网页同一时刻只运行一个生成任务，避免 RTX 5070 12GB 显存被多个大模型任务同时占满。
10. 启动脚本：重复双击启动脚本时，如果页面已在 7865 端口运行，会直接打开网页，不会重复启动服务。

## 3. 当前文件说明

| 文件或目录 | 用途 |
| --- | --- |
| `启动本地视频工作台.bat` | 启动入口。自动定位整合包 Python 并打开本地网页。 |
| `local_video_studio.py` | 网页、ComfyUI API 调用、任务等待和结果下载逻辑。 |
| `results/` | 自动保存生成的首帧、MP4 和人工抽取的预览图。 |
| `README.md` | 快速启动说明。 |
| `功能说明与扩展指南.md` | 本文档。 |

工作流原文件不在本目录，位置为：

```text
D:\zlyun\ZLY AI Video Studio\配置文件+工作流
```

## 4. 当前接入的工作流

### 4.1 文生图工作流

- 文件：`Flux2-Klein-文生图.json`
- 本地模型：`flux-2-klein-9b-fp8.safetensors`
- 文本编码器：`qwen_3_8b_fp8mixed.safetensors`
- VAE：`flux2-vae.safetensors`
- 无参考图时为标准文生图；上传一张图时，页面会动态加入 `LoadImage`、`ImageScaleToTotalPixels`、`VAEEncode` 和两个 `ReferenceLatent` 节点。
- 可选画幅：`1280 x 720`、`1024 x 1024`、`720 x 1280`

网页程序使用的核心节点映射：

| 作用 | ComfyUI 节点 ID |
| --- | --- |
| 正向提示词 | `2` |
| 负面提示词 | `3` |
| 画面尺寸 | `5` |
| 随机种子与采样 | `6` |
| 图片输出 | `8` |
| 参考图动态节点 | `10` 至 `14` |

### 4.2 首帧工作流

- 文件：`Flux2-Klein-三图参考.json`
- 本地模型：`flux-2-klein-9b-fp8.safetensors`
- 文本编码器：`qwen_3_8b_fp8mixed.safetensors`
- VAE：`flux2-vae.safetensors`
- 固定画幅：`1280 x 720`

网页程序使用的节点映射：

| 作用 | ComfyUI 节点 ID |
| --- | --- |
| 参考图 1 | `76` |
| 参考图 2 | `164` |
| 参考图 3 | `179` |
| 首帧提示词 | `108` |
| 随机种子 | `146` |
| 首帧输出 | `195` |

### 4.3 图生视频工作流

- 文件：`video_ltx2_3_i2v(new).json`
- 本地模型：`ltx-2.3-22b-dev-fp8.safetensors`
- 文本编码器：`gemma_3_12B_it_fpmixed.safetensors`
- 输出编码：MP4 / H.264
- 当前默认输出：`1280 x 704`、约 `5.04` 秒、`121` 帧、`24 fps`

网页程序使用的节点映射：

| 作用 | ComfyUI 节点 ID |
| --- | --- |
| 首帧输入图 | `98` |
| 视频运动提示词 | `167:164` |
| 两个随机噪声种子 | `167:135`、`167:165` |
| 视频输出 | `75` |

## 5. 启动和使用

前提：ComfyUI 整合包已运行，并可访问 `http://127.0.0.1:8188`。

1. 双击 `启动本地视频工作台.bat`。
2. 浏览器打开 `http://127.0.0.1:7865`。
3. 在“文生图”页填写图片提示词。可选上传一张参考图，再选择横版、方图或竖版后点击“生成图片”。
4. 在“首帧与视频”页上传三张参考图。建议分别提供场景、主体、服装/风格等关键参考，且三张图的视觉风格不要相互冲突。
5. 填写首帧提示词。先点击“生成高质量首帧”，确认构图、主体、时代和美术风格正确。
6. 填写视频运动提示词。只描述需要发生的动作和镜头运动，避免重复描述首帧已有的静态内容。
7. 点击“生成首帧并制作视频”。

局域网访问地址为：`http://本机IP:7865`。外网部署前应先加访问认证，不应直接将 7865 端口暴露到公网。

## 6. 已验证结果与性能

测试机器：NVIDIA GeForce RTX 5070，12GB 显存。

| 环节 | 验证结果 | 首次实测耗时 |
| --- | --- | --- |
| Flux2-Klein 文生图（1024 x 1024） | 成功生成并保存 PNG | 约 29 秒 |
| Flux2-Klein 参考图生图（1024 x 1024） | 成功生成并保存 PNG | 约 35 秒 |
| Flux2-Klein 三图首帧 | 成功生成并回传网页 | 约 1 分钟 |
| LTX 2.3 图生视频 | 成功生成 MP4 | 约 7 分钟 |

LTX 2.3 是 22B 模型，12GB 显存下会频繁在显存与系统内存之间卸载，因此速度显著低于在线平台。它适合作为高质量成片模型；后续建议再增加一个低分辨率、短时长或低帧数的“预览版”视频工作流，供同事先选镜头。

## 7. 新增工作流的接入步骤

新增工作流时，优先保持当前的“页面提交 API 工作流”模式，不需要改动 ComfyUI 内部插件。

1. 在 ComfyUI 中跑通新工作流，确认模型和自定义节点均已安装。
2. 使用 ComfyUI 的“保存 API 格式”导出 JSON，放入 `D:\zlyun\ZLY AI Video Studio\配置文件+工作流`。
3. 记录网页需要控制的节点 ID：图片输入、视频输入、正/负提示词、尺寸、帧数、种子和输出节点。
4. 在 `local_video_studio.py` 中增加工作流路径和节点 ID 常量。
5. 新增一个 `build_xxx_workflow()` 函数：深拷贝 JSON，仅替换需要由网页控制的输入值。
6. 新增一个生成函数：上传素材、调用 `/prompt`、轮询 `/history/{prompt_id}`、下载输出文件。
7. 在 `create_app()` 中增加对应的页面控件和按钮。
8. 用单独的小素材先验证一次，确认 ComfyUI 历史记录中输出节点的字段名为 `images`、`videos` 或 `gifs` 之一。
9. 完成后在本文档追加工作流文件名、模型、节点 ID、输出规格和实测耗时。

## 8. 新增工作流时的注意事项

- 不要直接修改现有 `Flux2-Klein-三图参考.json` 与 `video_ltx2_3_i2v(new).json` 的节点 ID；网页代码依赖这些 ID。
- 如需修改原工作流，建议另存为新文件，并在代码中建立新的节点映射，不影响现有生产链路。
- 必须使用 API 格式 JSON。普通前端工作流 JSON 不能直接提交到 `/prompt`。
- 每个可重复运行的工作流都应设置随机种子，避免缓存导致“点了生成但没有重新推理”。
- 如果新工作流使用多个大模型，仍应复用 `GENERATION_LOCK`，保证只运行一个任务。
- 输出节点必须将文件写入 ComfyUI 输出目录，并能出现在 `/history/{prompt_id}` 返回的 `outputs` 内。
- 需要面向同事开放时，先确定素材保留时长、结果目录容量、访问权限与日志清理策略。

## 9. 后续建议

1. 增加“预览版视频”工作流：降低分辨率或时长，用于快速看镜头运动。
2. 增加多个预设：文旅航拍、古风人物、建筑特写、人物讲解等，自动填充提示词模板。
3. 增加任务记录：保存参考图、提示词、种子、生成时间与结果文件，便于复现和筛选。
4. 增加登录认证后，再通过 Tailscale、Cloudflare Tunnel 或反向代理提供外网访问。
5. 配置结果定期归档或清理，防止 `results` 和 ComfyUI 输出目录持续占满磁盘。

## 10. 2026-08 最新架构基线

本节覆盖新版 React + FastAPI 工作台在 2026-08 的最新行为；后续 AI 进行修改时应同时阅读根目录 `AGENTS.md` 和 `docs/ARCHITECTURE.md`。

### 10.1 工作流选择替代模型选择

前端不再把“图片模型”作为主要选择项，而是从后端 `/api/modes` 获取工作流注册表。工作流能力、参考图上下限和参数校验统一由 `backend/app/workflow_registry.py` 管理。

### 10.2 MiniMax H3 动态参考图

- `minimax-h3-t2v`：无参考图。
- `minimax-h3-i2v`：首帧必填，尾帧可选。
- `minimax-h3-r2v`：支持 1-9 张有序参考图；前端可以排序并插入 `<Picture n>`。
- `minimax-h3-t8-all-reference`：支持 0-9 张有序参考图，使用多速率 T8 采样器。
- `minimax-h3-t8-dual-clock`：支持 0-1 张参考图，使用双时钟 T8 采样器，默认 8 步；单图按首帧 I2VA 生成。

原有 H3 graph 由 `backend/app/minimax_h3_workflow.py` 按实际上传数量生成；T8 graph 由 `backend/app/minimax_h3_t8_workflow.py` 生成。工作台不直接提交 ComfyUI 前端格式 JSON。各模式的 `options` schema 由 `workflow_registry.py` 提供，前端按 schema 动态显示、提交并回显参数。

### 10.3 运行与验证

工作台仍使用 `127.0.0.1:7865`，ComfyUI 仍使用固定实例 `127.0.0.1:8188`。每次跨模块修改后运行 `backend/tests/test_core.py` 和 `npm --prefix frontend run build`；涉及界面时检查桌面与移动端布局。

## 11. 2026-08-05 移除旧版 Gradio 启动入口

- 原因：React + FastAPI 工作台已成为唯一支持的用户入口，移除旧版 Gradio 的独立启动脚本以避免误启动旧界面。
- 受影响文件：删除 `启动旧版Gradio工作台.bat`；更新 `启动本地视频工作台.bat`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：`local_video_studio.py` 保留但不再提供启动入口；工作台仍通过 `启动本地视频工作台.bat` 在 `127.0.0.1:7865` 启动。
- 验证命令：`& "<ComfyUI Python>" backend/tests/test_core.py`、`npm --prefix frontend run build`。
- 回滚方式：从 Git 历史恢复 `启动旧版Gradio工作台.bat`，并恢复本文档和 `README.md` 中的旧版回退说明。

## 20. 2026-08-11 启动脚本自动清理遗留 Gradio

- 原因：启动脚本之前在 7865 已被占用时会直接打开该端口，导致仍在运行的 `local_video_studio.py` 旧版 Gradio 页面被误认为当前工作台。
- 实现：`启动本地视频工作台.bat` 仅在监听进程的命令行包含 `local_video_studio.py` 时将其结束，再启动 `backend.app.main:app`；其他进程占用端口时不做终止操作。`local_video_studio.py` 保留工作流辅助函数供新版后端复用，但不再能直接启动 Gradio，也不会让新版后端依赖 Gradio。
- 受影响文件：`启动本地视频工作台.bat`、`local_video_studio.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：用户入口继续是同一个批处理脚本和 `http://127.0.0.1:7865`；不修改 SQLite、任务队列、ComfyUI 地址或工作流协议。
- 验证命令：`& "<ComfyUI Python>" backend/tests/test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`；确认旧 Gradio 监听 7865 时双击脚本会启动新版工作台。
- 回滚方式：恢复批处理脚本中的端口处理分支，并删除本节及对应的 README、架构记录；不需要数据迁移。
## 12. 2026-08-05 生成进度同步

- 原因：生成视频期间只显示阶段文字，用户无法判断当前任务是否仍在运行。
- 实现：后端为每个 ComfyUI 提交建立同一客户端 ID 的 WebSocket 监听，读取 `progress_state` 的当前节点步数；任务 API 增加 `progress`（0-100），前端运行中的任务详情显示实时进度条。
- 多阶段任务：Flux 首帧阶段映射到 0-25%，LTX 视频阶段映射到 25-95%，任务完成固定为 100%。H3、VACE 和图片工作流按当前 ComfyUI 节点进度显示。
- 受影响文件：`backend/app/comfy_service.py`、`backend/app/storage.py`、`backend/app/worker.py`、`backend/app/models.py`、`frontend/src/App.tsx`。
- 兼容性与回滚：SQLite 自动迁移 `progress` 列；无法连接 WebSocket 时仍按原 HTTP 历史轮询完成任务，但进度保持阶段起始值。恢复上述文件即可回滚。
- 验证命令：`<ComfyUI Python> backend/tests/test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`。

## 13. 2026-08-06 后端 API 文档

- 原因：工作台已有 FastAPI 后端，增加随代码自动更新的接口文档，减少以手工文档或第三方项目为唯一来源造成的漂移。
- 实现：`backend/app/main.py` 将 OpenAPI JSON 固定在 `/api/openapi.json`，并提供 `/api/docs`（Swagger UI）与 `/api/redoc`（ReDoc）；`backend/app/models.py` 补充了健康检查、模式、媒体与任务响应模型，使 schema 可读且可导入。仓库内 `docs/API.md` 记录调用约定和示例。
- 工具选择：FastAPI 内置 OpenAPI + Swagger UI/ReDoc 为主，原因是无需额外部署、路由和 Pydantic 模型变更会自动反映。Apifox 作为可选导入目标，用于团队测试、Mock 和协作；以 `/api/openapi.json` 为唯一接口定义来源。
- 受影响文件：`backend/app/main.py`、`backend/app/models.py`、`backend/tests/test_core.py`、`docs/API.md`、`docs/ARCHITECTURE.md`、`README.md` 和本文档。
- 兼容性：既有 `/api/*` 业务接口、`127.0.0.1:7865` 工作台地址、`127.0.0.1:8188` ComfyUI 地址、SQLite 任务结构及工作流协议均未变更；仅增加文档端点和 schema 元数据。
- 验证命令：`<ComfyUI Python> backend/tests/test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、`Invoke-RestMethod http://127.0.0.1:7865/api/openapi.json`。
- 回滚方式：恢复上述文件；没有数据库迁移、模型下载或 ComfyUI 节点改动需要回滚。

## 14. 2026-08-06 工作台局域网访问

- 原因：允许同一可信局域网的设备通过本机 IPv4 地址使用创作工作台。
- 实现：启动脚本和 `backend/app/main.py` 的 Uvicorn 均监听 `0.0.0.0:7865`；本机继续通过 `127.0.0.1:7865` 访问，局域网…5 tokens truncated…ed…启工作台并检查活动任务状态。
- 回滚方式：恢复上述文件；新增 SQLite 列无需删除。

## 20. 2026-08-11 企业账号与员工电脑本地资源

- 原因：匿名局域网接口无法区分员工任务和资源，服务器 `results` 长期归档也会持续占用生成工作站磁盘。
- 实现：新增首次超级管理员初始化、HttpOnly 会话、CSRF、三级 RBAC、员工强制修改初始密码、账号启停/重置和操作审计。任务增加 `owner_user_id`，任务列表、单任务、参考图、作品和下载均按当前用户授权。
- 本地交付：生成输出进入 `data/staging`；Chrome/Edge 在用户点击选择目录后，通过 File System Access API 写入员工电脑的 `ZLY AI Video Studio/YYYY-MM` 子目录，目录句柄和资源相对路径只保存在该浏览器 IndexedDB。写入完成后前端调用 `delivered` 回执，后端删除 `data/staging` 和固定 ComfyUI `output` 中精确匹配的原始输出，再把输出标记为 `local`；路径越界或非 `output` 类型会拒绝清理并保留暂存供重试。不支持目录 API 的环境降级为普通下载，不自动清理暂存。
- HTTPS：File System Access API 仅允许安全上下文。工作站本机 `127.0.0.1` 可直接使用；其他局域网电脑必须通过受信任 HTTPS 访问。启动脚本识别 `ZLY_AI_VIDEO_STUDIO_SSL_CERTFILE` 和 `ZLY_AI_VIDEO_STUDIO_SSL_KEYFILE`，启用后同时打开安全 Cookie。
- 七牛云兼容：`backend/app/resource_storage.py` 维护 provider 边界，任务输出协议使用 `delivery_status/download_url`。七牛云实现应新增 provider 并在该边界处理对象上传、签名读取和删除，禁止把七牛 SDK 调用散落到工作流或业务路由。
- 受影响文件：`backend/app/auth.py`、`backend/app/resource_storage.py`、`backend/app/config.py`、`backend/app/storage.py`、`backend/app/models.py`、`backend/app/main.py`、`backend/app/comfy_service.py`、`frontend/src/api.ts`、`frontend/src/local-resource-store.ts`、`frontend/src/Root.tsx`、`frontend/src/App.tsx`、`frontend/src/main.tsx`、`启动本地视频工作台.bat`、测试和三份主文档。
- 兼容性：工作台 `7865`、固定 ComfyUI `127.0.0.1:8188`、节点 ID 和单 worker 保持不变。SQLite 启动时自动迁移；无归属历史任务在首次初始化时归首位超级管理员。旧 `results` 文件按有归属任务兼容交付。
- 验证命令：`<ComfyUI Python> backend/tests/test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并检查桌面与 390px 初始化、登录、强制改密、账号管理、目录授权提示和本地交付状态。
- 回滚方式：恢复上述文件并重新构建前端；新增 SQLite 结构可保留。未交付的 `data/staging` 文件可迁回 `results`；已完成回执并清理的文件只存在员工电脑，需要从员工授权目录恢复。

## 21. 2026-08-11 MiniMax H3 T8 全能参考与双时钟工作流

- 原因：将固定 ComfyUI 的 `user/default/workflows/MiniMax H3/MiniMax H3全能参考工作流.json` 和 `双时钟采样8步效果更好工作流.json` 转换为 ZLY AI Video Studio 可提交、可校验、可恢复和可回显的 API graph。
- 实现：新增 `minimax-h3-t8-all-reference` 与 `minimax-h3-t8-dual-clock`。全能参考支持 0-9 张图片并用 `auto` 任务类型在 `T2VA`/`Ref2VA` 间选择；双时钟支持 0-1 张图片。两个模式的完整参数契约均由注册表提供，创建页只展示产品化后的创作参数，技术参数由后端默认值托管并可在任务详情中折叠回显。
- 固定集成参数：`filename_prefix`、`save_output=true`、节点连接、输出节点 `14` 和工作台结果下载逻辑不向调用方开放，避免任意路径和结果丢失。源工作流里未连接的 `LoadAudio`、`UniBlockSwap` 等节点不进入 API graph，因为它们不影响源工作流输出。
- 受影响文件：`backend/app/models.py`、`backend/app/workflow_registry.py`、`backend/app/minimax_h3_t8_workflow.py`、`backend/app/main.py`、`backend/app/comfy_service.py`、`backend/tests/test_core.py`、`frontend/src/App.tsx`、`docs/API.md`、`docs/ARCHITECTURE.md`、`README.md` 和本文档。
- 兼容性：原有 H3 三个模式、公共任务端点、SQLite 和固定 `127.0.0.1:8188` 不变；新模式依赖该实例现有的 `comfyui-minimax-h3-audio-T8`、KJNodes、VideoHelperSuite 和对应模型/LoRA。源 JSON 未修改。
- 验证命令：`<ComfyUI Python> backend/tests/test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、以 `http://127.0.0.1:8188/object_info` 对照两个 graph 的节点输入；在 `http://127.0.0.1:7865` 检查桌面和 390px 参数交互。
- 回滚方式：删除两个 T8 模式和 `minimax_h3_t8_workflow.py`，恢复注册表、服务分发、前端控件、测试与三份文档；不需要迁移数据库或删除现有媒体。

## 22. 2026-08-11 工作流参数产品化分层

- 原因：源 ComfyUI 工作流的可调控件面向节点调试，不等于普通用户完成视频创作时需要理解的选项。
- 实现：`workflow_registry.py` 的每个 option 必须声明 `ui_group`。`primary` 仅用于比例、时长等必要创作决策；`advanced` 用于画质、随机种子等可选创作控制；`internal` 用于自动任务类型、采样、Shift、模型、VAE、LoRA、运行设备、显存与编码参数。`option()` 默认归为 `internal`，新增工作流不会因遗漏元数据而意外暴露技术参数。
- 前端行为：主创建区只读取 `primary`；“更多设置”只读取 `advanced`；`internal` 不进入创建表单。任务详情按 `request_parameters[].visibility` 区分“创作参数”和折叠的“运行参数”，提示词不再在详情卡内重复显示。
- 接入规则：技术参数需要开放时，先设计为普通用户可理解的语义预设，再在注册表或 graph 构建器中映射到底层值。禁止前端按工作流 ID 或参数名自行维护显示名单。
- 受影响文件：`AGENTS.md`、`backend/app/workflow_registry.py`、`backend/app/models.py`、`backend/app/main.py`、`frontend/src/App.tsx`、`backend/tests/test_core.py`、`docs/API.md`、`docs/ARCHITECTURE.md`、`README.md` 和本文档。
- 兼容性：所有既有 `options` 仍可由 API 提交并执行；任务响应只新增参数可见性并回显完整有效值。无数据库迁移，不修改 ComfyUI 节点、模型路径、任务队列或固定端口。
- 验证命令：`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并在桌面与 390px 视口验证主参数、更多设置、运行参数折叠和无横向溢出。
- 回滚方式：恢复上述注册表、API、前端、测试和文档；已有 SQLite 数据、任务和媒体不需处理。

## 23. 2026-08-11 密码可见性与重置解锁

- 原因：员工和管理员需要核对较长密码，同时管理员重置密码后不应继续等待旧的 15 分钟登录冷却。
- 实现：初始化、登录、首次改密、创建员工和重置密码字段统一加入显示/隐藏图标；`POST /api/admin/users/{user_id}/reset-password` 成功后按账号清除所有 IP 的内存登录失败记录，并把清除数量写入审计明细。
- 受影响文件：`frontend/src/Root.tsx`、`backend/app/main.py`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：不修改请求或响应字段、SQLite 结构、账号角色、工作流、任务队列、模型路径以及 `7865`/`8188` 固定端口；其他账号的限流记录不受影响。
- 验证命令：`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并在桌面与 390px 视口验证显示/隐藏按钮和重置后的登录行为。
- 回滚方式：恢复上述代码、测试和文档文件；无需迁移或清理账号数据库。

## 24. 2026-08-11 首次目录配置强制引导

- 原因：首次改密后若未选择员工电脑的存储目录，用户仍可直接创建任务，生成结果无法进入预期的本地交付链路。
- 实现：`frontend/src/App.tsx` 按当前用户恢复 IndexedDB 中的目录句柄和授权状态。没有已授权目录、权限已失效或环境不支持目录 API 时，工作台显示不可关闭的目录配置层并禁止提交任务；选择并授予写入权限后自动进入工作台。该层提供重新授权与退出登录，不提供跳过入口。
- 受影响文件：`frontend/src/App.tsx`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：已授权目录用户仍会自动进入工作台；目录句柄和本地绝对路径不离开浏览器 IndexedDB。API、SQLite、任务队列、资源交付回执、模型路径、ComfyUI 节点和固定 `7865`/`8188` 端口均保持不变。
- 验证命令：`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并在 `http://127.0.0.1:7865` 检查桌面和 390px 视口下首次改密后的目录选择、拒绝授权、重新授权和已授权回访。
- 回滚方式：恢复上述前端和文档文件并重新构建；无需数据库迁移、任务清理或媒体恢复。

## 25. 2026-08-11 ZLYUN AI Tauri 桌面客户端

- 原因：员工通过局域网 HTTP 访问时，浏览器不能授予目录写入权限；同时企业后续需要可靠交付、受控本机能力和可升级的客户端基础。
- 实现：新增 `desktop` Tauri 子工程。客户端以 `ZLYUN AI` 显示，使用提供图片中裁出的蓝色图形作为无文字图标。它只加载指定的企业 HTTPS 工作台 origin；`desktop-workbench` capability 将本机命令限制给该 origin。前端检测 Tauri 后先通过已有 Cookie + CSRF 获取绑定当前输出、5 分钟有效的临时下载凭证，再由 Rust 分块下载并原子写入固定 `ZLY AI Video Studio/YYYY-MM` 子目录，通过 asset protocol 显示本地媒体；非 Tauri 环境完全保留浏览器目录句柄实现。
- 安全边界：Rust 拒绝绝对路径、`..` 组件和带目录的文件名；根目录由员工本机选择，按账号保存于应用数据目录，绝对路径不提交到工作台。部署者必须将 `lib.rs` 中的 `WORKBENCH_URL` 与 capability 中的 `remote.urls` 同时换成同一个受信任 HTTPS 域名，禁止放宽为通配域或 HTTP。
- 受影响文件：`desktop/`、`frontend/src/local-resource-store.ts`、`frontend/package.json`、`pnpm-workspace.yaml`、`.gitignore`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：既有浏览器工作台、账号 Cookie/CSRF、SQLite、任务 API、固定 `7865`、固定 ComfyUI `127.0.0.1:8188`、工作流协议和节点 ID 均不变；只新增客户端交付通道。
- 验证命令：`pnpm install`、`pnpm --filter zly-ai-video-studio-webui build`、`pnpm --dir desktop run build` 与 `<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`。在员工电脑通过受信任 HTTPS 打开客户端，验证选目录、生成结果自动写入、历史预览和交付回执。
- 回滚方式：停止分发客户端，恢复 `local-resource-store.ts` 的浏览器写入分支，并重新构建 Web 前端；无数据库迁移、模型下载、工作流或媒体删除操作。

## 26. 2026-08-11 MiniMax H3 参数组件化

- 原因：标准 H3 三种视频工作流的画面比例和时长曾使用原生文本/数字输入，无法与工作台选择控件保持一致。
- 实现：注册表为标准 H3 画面比例提供 `ui_control=select` 和预设 `ui_options`；前端直接使用 Ant Design 的 `Select`、`InputNumber`、`Switch` 按 schema 渲染，避免按参数名硬编码。数值控件会遵守注册表的最小值、最大值、步长和单位。
- 受影响文件：`backend/app/workflow_registry.py`、`frontend/src/App.tsx`、`frontend/src/main.tsx`、`frontend/src/index.css`、`frontend/package.json`、`backend/tests/test_core.py`、README 与架构文档。
- 兼容性：现有 `options` 请求体、H3 自定义合法比例 API、SQLite、任务队列、ComfyUI 节点和固定 `7865`/`8188` 端口均不变；模式接口仅附加可忽略的 UI 元数据。
- 验证：`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，以及在 `http://127.0.0.1:7865` 的桌面和 390px 视口检查下拉、步进与开关状态。
- 回滚：恢复本次注册表、Ant Design 前端接入、测试和本文档改动并重新构建前端；无需迁移或清理数据库、模型、ComfyUI 工作流和媒体。

## 27. 2026-08-11 Docker 服务器部署

- 原因：需要将当前 React + FastAPI 工作台部署到服务器，同时继续使用已运行在 `https://comfyui.zlyun168.com`、服务器端口 `18188` 的 ComfyUI。
- 实现：新增 `Dockerfile`、`compose.yaml`、`.env.example`、`.dockerignore`、`backend/requirements.txt` 和 Nginx `18189` HTTPS 配置示例。前端在镜像构建期生成静态产物，FastAPI 在同一运行镜像托管；ComfyUI、模型和自定义节点均不进入镜像。Compose 使用命名卷持久化数据库与临时资源，绑定挂载 ComfyUI `output` 以完成已交付结果的精确清理，并以只读挂载提供 Flux/LTX 工作流 JSON。`ZLY_AI_VIDEO_STUDIO_WORKFLOW_DIR` 让容器可指定该目录，旧本地 Windows 默认路径仍保留。
- 受影响文件：`Dockerfile`、`compose.yaml`、`.env.example`、`.dockerignore`、`.gitignore`、`docker/nginx/zly-ai-video-studio.conf.example`、`backend/requirements.txt`、`local_video_studio.py`、`README.md` 和 `docs/ARCHITECTURE.md`。
- 兼容性：工作台容器内 API 仍为 `7865`，服务器 Nginx 以 HTTPS `18189` 对外暴露；现有 API、数据库、任务队列、节点 ID、模型路径与唯一 ComfyUI 实例均不改变。工作台通过 `ZLY_AI_VIDEO_STUDIO_COMFY_URL` 调用既有服务，不会启动第二套 ComfyUI。
- 验证命令：`docker compose config`、`docker compose build`、`docker compose up -d`、`curl http://127.0.0.1:7865/api/health`、`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py` 和 `pnpm --filter zly-ai-video-studio-webui build`。
- 回滚方式：`docker compose down` 后恢复上一镜像并重新 `up -d`；不执行 `docker compose down -v`，以保留 SQLite、上传和暂存资源。移除 Nginx `18189` 虚拟主机后重载 Nginx 即可关闭对外入口。

## 28. 2026-08-11 ZLY AI 视频创作平台单域名部署

- 原因：将工作台品牌统一为 `ZLY AI Video Studio` / “ZLY AI 视频创作平台”，并让用户只通过 `https://comfyui.zlyun168.com/` 访问前端和 `/api` 后端；避免将 ComfyUI API 暴露给公网。
- 实现：Compose 项目、服务、镜像、容器和持久化卷命名为 `zly-ai-video-studio`。Linux 服务器使用 host network，Uvicorn 只监听 `127.0.0.1:18189`，直接访问同机 `127.0.0.1:18188` ComfyUI；Nginx 根路径与 `/api` 统一代理到工作台，ComfyUI 无外部 location。前端和桌面端显示名及受信任 HTTPS origin 同步为 ZLY AI 与 `https://comfyui.zlyun168.com/`。
- 受影响文件：`Dockerfile`、`compose.yaml`、`.env.example`、`docker/nginx/zly-ai-video-studio.conf.example`、`docker/nginx/zly-ai-video-studio.conf.example`、`backend/app/config.py`、`backend/app/comfy_service.py`、`backend/app/main.py`、`frontend/src/App.tsx`、`frontend/src/Root.tsx`、`desktop/src-tauri/src/lib.rs`、`desktop/src-tauri/capabilities/desktop-workbench.json`、`README.md` 和 `docs/ARCHITECTURE.md`。
- 兼容性：浏览器和本地开发继续使用 `/api`；SQLite schema、任务队列、节点 ID、模型路径及 API 请求格式不变。服务器不再通过 `18189` 或公网 `/api` 暴露 ComfyUI；部署需要 Linux Docker host-network 支持。
- 验证命令：`docker compose config`、`docker compose build`、`curl http://127.0.0.1:18189/api/health`、`curl -I https://comfyui.zlyun168.com/api/health`、`<ComfyUI Python> -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`。
- 回滚方式：停止新容器后使用原 `zly-ai-video-studio-workbench` 镜像和旧 Compose/Nginx `18189` 配置恢复；新命名卷不会删除旧 `zly-ai-video-studio-data` 卷，必要时按备份迁移 SQLite、上传与暂存数据。

## 29. 2026-08-11 移除旧 Flux、LTX 与 Wan VACE 工作流

- 原因：工作台后续只提供 MiniMax H3 系列视频创作能力，不再维护 Flux2-Klein 图片生成、Flux + LTX 2.3 视频和 Wan VACE 三图视频。
- 实现：从 `workflow_registry.py` 移除三个模式，前端由 `/api/modes` 动态读取后不再显示对应入口；`compose.yaml`、`Dockerfile` 与 `.env.example` 删除 `ZLY_AI_VIDEO_STUDIO_WORKFLOW_DIR`、`/workflows` 和旧 JSON 挂载。未完成旧任务会在应用启动恢复时标记失败，已完成历史任务仍保留浏览和下载。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/main.py`、`backend/app/worker.py`、`backend/app/models.py`、`backend/tests/test_core.py`、`frontend/index.html`、`frontend/src/App.tsx`、`Dockerfile`、`compose.yaml`、`.env.example`、`.dockerignore`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：H3 模式、`/api`、SQLite schema、ComfyUI `127.0.0.1:18188` 与服务器 `127.0.0.1:18189` 均不变；旧模式不再可新建，旧任务仅保留已完成结果。
- 验证命令：`docker compose --env-file .env.example config`、`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、`docker compose build`、`curl http://127.0.0.1:18189/api/health`。
- 回滚方式：恢复上一版镜像、Compose、`.env` 和三个旧 JSON 挂载后重新启动；不执行 `docker compose down -v`，以保留 SQLite、上传素材与暂存结果。

## 2026-08-11 运行环境变量品牌迁移

- 原因：服务器部署配置需要与产品名 `ZLY AI Video Studio` 保持一致。
- 受影响文件：`compose.yaml`、`.env.example`、`Dockerfile`、`backend/app/config.py`、本地启动脚本和三份部署文档。
- 兼容性：端口、API、SQLite、任务队列、ComfyUI 地址和模型路径不变；部署前必须将 `.env` 与服务管理器中的同用途变量更名为 `ZLY_AI_VIDEO_STUDIO_*`。
- 验证命令：`docker compose --env-file .env.example config`、`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`。
- 回滚方式：恢复本次涉及的配置、镜像和启动脚本文件；无需迁移或清理数据库与媒体文件。

## 30. 2026-08-11 browser-stream 远端 ComfyUI 成片交付

- 原因：ComfyUI 部署在远端电脑、通过 FRP 映射到服务器 `127.0.0.1:18188` 时，完整视频先下载到工作台服务器会浪费服务器磁盘。
- 实现：新增默认 `browser-stream` provider。H3 完成时仅持久化 ComfyUI 输出引用；浏览器或桌面端请求既有受保护下载 URL 后，FastAPI 才从 `/view` 流式读取并直接写入员工本机目录。ComfyUI URL、文件名和目录信息不会出现在任务 API 响应中。Compose 删除 `COMFY_OUTPUT_DIR` 及其 `output` 绑定挂载。
- 受影响文件：`backend/app/resource_storage.py`、`backend/app/comfy_service.py`、`backend/app/main.py`、`backend/app/config.py`、`backend/tests/test_core.py`、`Dockerfile`、`compose.yaml`、`.env.example`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：任务 API、SQLite、H3 工作流、节点 ID、账号权限、本地目录和桌面交付协议不变；`browser-local` 保留给旧部署或回滚使用。远端 ComfyUI `output` 由运行 ComfyUI 的电脑负责按时间或容量清理；工作台不再尝试删除它。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、`docker compose --env-file .env.example config`、`docker compose build`。
- 回滚方式：恢复上一版镜像与 Compose，重新配置 `COMFY_OUTPUT_DIR` 并将 provider 改为 `browser-local` 后重启；不执行 `docker compose down -v`，以保留 SQLite 和上传素材。

## 31. 2026-08-12 Nginx 无磁盘缓冲反向代理

- 原因：Nginx 的默认代理缓冲可能将 `browser-stream` 的视频响应写入临时文件，抵消服务器不保存成片的设计目标。
- 实现：更新 `docker/nginx/zly-ai-video-studio.conf.example`。HTTP 强制跳转 HTTPS；HTTPS 仅代理工作台 `/` 与 `/api/` 至 `127.0.0.1:18189`，`/api/auth/setup` 只允许回环来源；`/api/` 关闭 request/response buffering 与临时文件，超时设置为 600 秒。配置中不包含任何 ComfyUI 或 FRP `18188` 的公开代理。
- 受影响文件：`docker/nginx/zly-ai-video-studio.conf.example`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：容器、`browser-stream`、同源 Cookie/CSRF、`127.0.0.1:18188` FRP 映射和 `/api` 路径保持不变。部署时仅替换域名和证书绝对路径；防火墙只开放 80/443。
- 验证命令：`sudo nginx -t`、`sudo systemctl reload nginx`、`curl -I https://<工作台域名>/api/health`、`ss -lntp | grep -E '18188|18189'`。
- 回滚方式：恢复上一版 Nginx 虚拟主机并重载 Nginx；不会删除工作台数据卷、远端 ComfyUI 输出或员工本地资源。


## 2026-08-11 品牌与运行数据迁移

- 原因：移除项目自有运行标识中的旧品牌名称，统一使用 `ZLY AI Video Studio`。
- 受影响文件：后端配置与认证、前端本地资源存储、桌面端、Docker、启动脚本、构建包名、工具脚本及三份文档。
- 兼容性：应用启动时自动迁移旧数据库和 WAL/SHM 文件；浏览器本地资源记录自动导入新 IndexedDB；Cookie 与 CSRF 盐值已更新，用户需要重新登录。已有上传素材、任务记录、ComfyUI 模型与节点不迁移、不删除。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、`pnpm --dir desktop run build`。
- 回滚方式：恢复本次代码与配置；已迁移的数据库文件可改回原文件名，媒体文件无需处理。

## 31. 2026-08-12 Docker 配置模块启动修复

- 原因：Docker 容器中的工作台根目录为 `/app`，旧代码通过固定祖先索引推导本地 ComfyUI 默认目录，导入 FastAPI 应用时即发生 `IndexError`，因此服务尚未监听 `127.0.0.1:18189` 就退出。
- 受影响文件：`backend/app/config.py`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 实现：默认本地 ComfyUI 根目录改为从工作台目录父级推导；Docker 继续只通过 `ZLY_AI_VIDEO_STUDIO_COMFY_URL=http://127.0.0.1:18188` 调用已有唯一 ComfyUI。
- 兼容性：端口、`/api`、SQLite、任务队列、ComfyUI 节点与工作流协议均不变。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`docker compose build`、`docker compose up -d`、`curl http://127.0.0.1:18189/api/health`。
- 回滚方式：恢复上述配置、测试和文档文件后构建上一镜像并启动；不要执行 `docker compose down -v`，以保留数据卷。

## 32. 2026-08-12 tar 镜像一键部署脚本

- 原因：离线 tar 更新需要连续执行加载镜像、重建容器、查看状态和健康检查，手动执行容易遗漏关键参数或误清理数据卷。
- 实现：新增 `deploy-server-image.sh`。脚本优先读取项目目录内 `packages/zly-ai-video-studio_latest.tar`，并回退兼容同目录归档，也接受绝对路径参数；检查 Docker、Compose、curl 和 `compose.yaml` 后加载镜像，以 `--no-build --force-recreate` 重建工作台容器，并在 30 秒内检查 `127.0.0.1:18189/api/health`。失败时输出容器最近 100 行日志。
- 受影响文件：`deploy-server-image.sh`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：脚本不执行 `docker compose down` 或 `down -v`，因此账号、SQLite、上传素材和任务数据卷不变；端口、API、ComfyUI 地址、工作流和任务队列不变。
- 验证命令：在 Linux 服务器项目目录执行 `bash deploy-server-image.sh`。
- 回滚方式：上传上一版 tar，并以其绝对路径作为参数再次执行脚本；不要执行 `docker compose down -v`。

## 33. 2026-08-12 首位管理员容器初始化命令修正

- 原因：部署文档原先用 `python - <<'PY'` 提供 Python 脚本，会使 `input()` 和 `getpass()` 无法读取 SSH 终端的输入，并在输入账号时出现 `EOFError`。
- 实现：文档改为 `python -c` 执行初始化逻辑，脚本源码通过命令参数提供，账号、姓名和密码继续由容器终端交互读取；密码不会出现在命令历史中。
- 受影响文件：`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：`/api/auth/setup` 仍只接受容器回环地址，Nginx 仍拒绝公网初始化；账号、SQLite、会话、端口和 ComfyUI 连接均不变。
- 验证命令：在服务器执行 `docker compose exec zly-ai-video-studio sh`，再按 README 的 `python -c` 命令完成首位管理员创建；以该账号从 HTTPS 工作台登录。
- 回滚方式：恢复上述三份文档；不涉及镜像、容器重建或数据迁移。

## 35. 2026-08-12 画质预设与随机种子产品化

- 原因：MP 和随机种子是 ComfyUI 的实现参数，普通创作用户无需理解或调整。
- 实现：`workflow_registry.py` 将画质定义为 `1K`、`2K`、`4K` 的选择预设，并按工作流映射内部 `megapixels`；T8 的 `seed` 回归 `internal`，每次建任务由后端自动生成。前端只提交主参数与高级创作参数，任务详情默认显示 K 档位，内部 MP 与种子放入运行参数。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/main.py`、`frontend/src/App.tsx`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：ComfyUI graph 的 MP 和种子输入、SQLite、API 路由、节点 ID、模型路径、任务队列以及 `7865`/`8188` 固定端口不变；已有仅含 MP 的任务会按最近 K 档位回显。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并在 `http://127.0.0.1:7865` 的桌面与 390px 视口检查画质下拉、更多设置和运行参数。
- 回滚方式：恢复上述注册表、API、前端、测试和文档文件后重新构建；不需要迁移或清理 SQLite、媒体、模型或 ComfyUI。

## 34. 2026-08-12 browser-stream ComfyUI 宿主机浏览器直连

- 原因：ComfyUI 宿主机上的浏览器保存成片时，原实现需要经服务器和 FRP 读取同一台机器的文件，浪费带宽和时间。
- 实现：后端新增 `GET /api/jobs/{job_id}/outputs/{output_index}/browser-direct`，只向已授权任务所有者返回固定 `http://127.0.0.1:8188/view` 的受控输出定位地址。浏览器目录交付先请求本机 ComfyUI，并通过 `ReadableStream` 分块写入已授权目录；未命中同机 ComfyUI、CORS 限制或任意本机读取错误时，自动回退原 `/download` 的服务器流式交付。任务 API 和作品库仍不输出 `_comfy_source`。
- 运行要求：远端 ComfyUI 宿主机须用 `Start-ComfyUI.cmd --enable-cors-header https://<实际工作台域名>` 启动；该值必须是完整的 HTTPS Origin，不能使用 `*`。ComfyUI 保持 `127.0.0.1:8188` 回环监听，不新增公网接口或第二套 ComfyUI。
- 受影响文件：`backend/app/main.py`、`backend/app/models.py`、`frontend/src/local-resource-store.ts`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：服务器流式交付、桌面客户端、SQLite、资源 Provider、工作流、节点 ID、模型路径、FRP 和端口不变。非宿主机浏览器无需配置，会继续使用服务器交付。
- 验证：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`、`docker compose build`；在宿主机 Chrome/Edge 确认网络响应来自 `127.0.0.1:8188/view`，在非宿主机确认回退 `/api/jobs/.../download`。
- 回滚：恢复本次代码与文档并重新构建，或移除 ComfyUI 的 CORS 启动参数后重启实例；无需迁移数据库、任务或媒体。
## 2026-08-12 ComfyUI/FRP 断线自动释放队列

- 原因：ComfyUI 崩溃或 FRP 短暂重启后，旧任务可能持有已失效的 `prompt_id`，使单 worker 持续等待而阻塞新任务。
- 实现：工作台对 ComfyUI 连接、上传、提交和任务轮询设置连续 30 秒的断线判定；断线或重启后找不到原任务时，将当前任务标记为“已中断”，清除执行标识并继续后续队列。安全失败且已清除执行标识的 H3 任务也可在任务详情点击“重新提交”，只有用户确认 ComfyUI 和 FRP 已恢复后才会重新入队。
- 受影响文件：`backend/app/comfy_service.py`、`backend/app/worker.py`、`backend/app/storage.py`、`backend/app/main.py`、`frontend/src/App.tsx`、`backend/tests/test_core.py`、`README.md` 和 `docs/ARCHITECTURE.md`。
- 兼容性：不新增 ComfyUI 实例、不变更 8188/18188/7865/18189 端口、不修改工作流节点或现有任务创建接口；未自动重试已提交任务，避免重复生成。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`。
- 回滚方式：恢复上述代码和文档文件的前一版本；SQLite、上传素材、现有任务与 ComfyUI 输出无需回滚或清理。

## 36. 2026-08-12 参数栏图标化与时长滑杆

- 原因：比例、分辨率和时长是高频创作选择，原先分散的下拉菜单和数字步进器不利于快速预览与切换。
- 实现：工作流 schema 为画面比例声明 `ui_control="visual-settings"` 与 `ui_companion="quality"`；前端以画幅图标列表显示所有注册表下发的比例，并在同一 Popover 内显示分辨率预设。时长使用 `ui_control="duration-slider"`，同一 Popover 中同时提供刻度滑杆与 Ant Design `InputNumber` 手动输入。
- 受影响文件：`backend/app/workflow_registry.py`、`frontend/src/App.tsx`、`frontend/src/index.css`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：前端不依赖参数名，而是根据 schema 元数据渲染；提交值、后端校验、ComfyUI graph、数据库、端口和历史任务均不变。旧客户端会忽略新增展示元数据。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`。
- 回滚方式：恢复本次注册表、前端、测试和文档修改；无需迁移或清理任何 SQLite、媒体、模型或 ComfyUI 资源。

## 37. 2026-08-12 本地 Vite 热更新启动

- 原因：原启动脚本仅在缺少 `frontend/dist` 时构建一次，之后由 FastAPI 托管静态产物；修改前端源码不会反映到浏览器，开发时必须手动重启。
- 实现：`启动本地视频工作台.bat` 在独立终端启动 Vite 并等待 `127.0.0.1:5173` 可用，再打开该地址；Vite 固定将 `/api` 转发至 `127.0.0.1:7865`，前端源码保存后自动热更新。脚本退出时会结束它启动的 Vite 进程树。设置既有 SSL 环境变量时，Vite 使用同一证书并代理 HTTPS FastAPI。
- 受影响文件：`启动本地视频工作台.bat`、`frontend/vite.config.ts`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：FastAPI、ComfyUI、任务队列、SQLite、工作流节点、模型路径与 `7865`/`8188` 固定端口均不变；`frontend/dist` 和 Docker 生产部署继续由 FastAPI 托管。Vite 仅监听本机回环地址，局域网访问模式不变。
- 验证命令：`pnpm --dir frontend build`；双击脚本并访问 `http://127.0.0.1:5173`，修改 `frontend/src/App.tsx` 或样式后确认页面无需重启即可更新。
- 回滚方式：恢复上述启动脚本、Vite 配置和文档，再执行 `pnpm --dir frontend build`；无需回滚数据库、上传素材、任务或 ComfyUI。
## 38. 2026-08-13 生图/生视频统一创作工作台

- 原因：复用 Smart-Floor-Planner 已验证的 GRS 协议，给现有暗色创作台增加图片模式，同时统一图片与视频的多轮任务语义。
- 实现：新增 GPT Image 2 / VIP 注册表定义和声明式参数条件；图片支持 0–10 张有序参考图、每轮 1–4 个生成项、比例/分辨率/VIP 自定义尺寸。任务详情按轮次展示，图片可只重试失败项、再次生成下一轮，并可从已交付原图创建带来源关联的视频任务。管理设置仅向超级管理员显示 GRS 加密凭证、模型开关、连接测试和余额。
- 执行与安全：视频保持 ComfyUI 串行，图片最多 4 并发；远端 ID 先持久化再轮询，临时错误退避至 30 秒，总超时 30 分钟。下载逐跳检查 HTTPS 与公网 DNS/IP，并校验 MIME、签名和 50 MB 上限。真实 GRS 生成只在明确批准费用后执行。
- 数据迁移：新增 `schema_migrations`、`job_rounds`、`generation_items` 与 `grs_provider_settings`；迁移前自动备份 SQLite。旧视频原地生成首轮/首项并保留 ID/输出/执行标识，旧 Flux 图片仅只读。顶层字段与旧输出 API 映射最新轮次以兼容旧调用。
- 前端：品牌显示为 `ZLY AI Studio｜创作工作台`，图片/视频使用 Ant Design `Segmented` 切换并分别保存草稿；图片结果、视频结果和 GRS 管理设置分别懒加载。新作品目录为 `ZLY AI Studio/<YYYY-MM>`，旧目录和 IndexedDB 继续可读。
- 受影响文件：`backend/app` 的模型、注册表、存储、GRS、worker 与 API，`frontend/src` 的创作台、管理页、媒介模块和本地资源存储，`desktop/src-tauri/src/lib.rs`、测试、环境变量、Compose 和三份主文档。
- 兼容性：数据库文件名、环境变量前缀、包名、固定端口、ComfyUI 实例、节点 ID、模型路径和旧 API 保持；缺少凭证主密钥时只锁定生图。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --filter zly-ai-video-studio-webui build`、`docker compose --env-file .env.example config`，以及桌面端与 `390×844` 页面回归和 Impeccable 检测。
- 回滚方式：停止工作台，恢复上一版代码/镜像和迁移前 SQLite 备份；不删除员工本地作品、上传素材或 ComfyUI 输出。也可仅在管理页关闭 GRS，以保留新 schema 和历史。

## 39. 2026-08-13 图片比例与分辨率弹窗

- 原因：图片工作台需要与视频工作台统一高频创作参数的选择方式。
- 实现：`workflow_registry.py` 为 GPT Image 2 和 GPT Image 2 VIP 的 `aspect_ratio` 声明 `visual-settings` 组合控件，并通过 `ui_companion="resolution"` 将分辨率放入同一弹层；比例选项和 VIP 独有的超长画幅均由注册表下发。前端不按模型或参数名分支，复用既有 Ant Design Popover 渲染。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：图片提交参数、GRS 尺寸映射、自定义尺寸、SQLite、任务队列、API 路由、ComfyUI 节点、模型路径及固定端口不变；旧客户端可忽略新增展示元数据。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --filter zly-ai-video-studio-webui build`，并检查桌面与 `390×844` 视口的弹窗选择和 VIP 自定义尺寸展示。
- 回滚方式：恢复本次注册表、测试和文档文件；无需迁移或清理数据库、任务、媒体或模型。

## 40. 2026-08-13 GRS 凭证与连接测试修复

- 原因：本地启动没有注入 Fernet 主密钥，连接测试又只读取数据库中的已保存 Key，导致表单内正确的 GRS Key 没有真正发送到上游验证。
- 实现：Windows 启动脚本首次创建并复用 `data/credential.key`；显式 `ZLY_AI_VIDEO_STUDIO_CREDENTIAL_KEY` 保持最高优先级。管理页测试连接时提交当前 Base URL/API Key，成功测试后再由管理员点击保存。余额请求完全复用 Smart-Floor-Planner 的协议：JSON body `{ apiKey }`，不发送 Authorization 头。
- 受影响文件：`启动本地视频工作台.bat`、`backend/app/local_credential_key.py`、GRS 客户端/服务/API 模型、供应商管理页、测试与三份主文档。
- 兼容性：无数据库迁移；旧测试接口无请求体仍测试已保存配置；视频、历史、ComfyUI、端口和图片生成任务协议不变。主密钥文件位于已忽略的 `data/`，不得在已有加密凭证时随意删除。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --filter zly-ai-video-studio-webui build`，并检查管理页凭证状态、当前输入测试和错误反馈。
- 回滚方式：恢复上述代码并重启工作台；无需回滚 SQLite。若回滚后仍需读取已保存 GRS Key，应继续保留并注入同一 Fernet 主密钥。

## 41. 2026-08-14 七牛云媒体存储

- 原因：接入 Smart-Floor-Planner 的七牛云媒体存储模式，使生成图片和视频拥有统一的持久化归档位置。
- 受影响文件：`backend/app/qiniu_storage.py`、`backend/app/qiniu_provider.py`、`backend/app/resource_storage.py`、`backend/app/storage.py`、`backend/app/main.py`、`backend/app/models.py`、`frontend/src/admin/QiniuStorageSettings.tsx`、`frontend/src/Root.tsx`、`backend/requirements.txt` 与测试。
- 用户行为：超级管理员在“管理设置 → 媒体存储”配置并测试七牛云后，勾选“启用七牛云”即可生效；图片和视频的最终副本先上传七牛云，已交付到员工本地的副本不会影响云端对象。
- 运行要求：本地启动脚本会为内置 Python 检查并安装 `qiniu==7.16.0`，Docker 镜像从后端依赖文件安装同一版本。
- 兼容性：关闭开关立即恢复既有 `browser-stream`/`browser-local` 策略；存量任务不迁移，七牛云历史对象保持可访问。
- 验证命令：`python -m unittest discover -s backend/tests -v`；`pnpm --dir frontend build`；在 `http://127.0.0.1:7865` 的超级管理员“媒体存储”页检查开关与移动端布局。
- 回滚方式：关闭七牛云开关并保存；需要代码回滚时恢复本次文件和 Python 依赖，不批量删除已上传对象。

## 42. 2026-08-14 GRS 上游余额创作页展示

- 原因：员工在 AI 生图前需要了解管理员最近查询到的 GRS 上游余额，避免切换到管理设置页查看。
- 实现：`GET /api/providers/grs/balance` 会在后端受控刷新上游余额并返回快照和查询时间；图片生成页在生成期间每 5 秒轮询、空闲时每 15 秒轮询。后端以 10 秒短缓存合并并发请求，并在每张图片成功后异步刷新，避免生图完成后余额长期不变。超级管理员仍可在供应商设置手动查询。
- 受影响文件：`backend/app/models.py`、`backend/app/grs_provider.py`、`backend/app/main.py`、`frontend/src/App.tsx`、测试与三份主文档。
- 兼容性：SQLite、GRS 生成协议、凭证保管、任务队列、ComfyUI 节点、模型路径和固定端口不变；旧客户端不会调用新接口。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并在桌面与 `390×844` 视口切换到“图片生成”确认余额状态可见。
- 回滚方式：恢复上述后端、前端和文档文件并重启工作台；不需要清理 SQLite，既有余额快照可继续保留。

## 44. 2026-08-14 即梦式任务会话栏与任务元数据

- 原因：任务列表要显示封面，并支持置顶、重命名、删除；生成类型切换改为创作面板中的下拉选择。
- 实现：任务行优先使用生成结果作为封面，没有结果时回退到参考图；悬浮显示操作入口，菜单提供置顶、重命名、删除。后端新增任务元数据更新和删除接口，置顶任务优先展示。
- 兼容性：旧任务自动获得未置顶状态；生成中的任务不能删除；工作流注册表、ComfyUI 节点和固定端口不变。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并在 Chrome 中用 DOM/computed style 检查任务行封面与菜单交互。
- 回滚方式：恢复本次后端、前端和文档变更并重启工作台；无需清理 SQLite。

## 45. 2026-08-14 即梦式右侧创作工作台

- 原因：生成区需要把提示词、参考图和参数收拢为更接近即梦的白色创作卡，降低当前界面的空隙和深色控件干扰。
- 实现：创作卡使用白色圆角表面和轻边框；首尾帧参考槽位保持 106px 高度；底部工具栏统一为 36px，并按生成类型、工作流、画面比例/分辨率、时长和提交按钮排列。图片模式和视频模式仍通过同一生成类型下拉切换。
- 兼容性：不改变工作流注册表、参数校验、任务协议和 ComfyUI 节点；已有图片、视频任务和上传参考图不受影响。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并在 Chrome 中通过 DOM/computed style 检查桌面创作卡和图片/视频切换。
- 回滚方式：恢复本次前端和文档变更并重建前端；无需清理 SQLite。

## 47. 2026-08-14 即梦式媒体图标与视频参数栏修复

- 原因：媒体下拉缺少图标，视频模型名称被截断并出现横向滚动，时长“5秒”换行，1K/2K 按钮 hover 时文字不可读。
- 实现：生成类型选项加入图片/视频图标；视频模型控件和下拉菜单加宽并取消文本省略；时长控件保持单行；比例弹窗中的分辨率选项改为深色文字、蓝色选中态和浅色 hover。
- 兼容性：不改变工作流和参数协议，只调整创作区控件和 portal 弹层样式。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并在 Chrome 中通过 DOM/computed style 检查。
- 回滚方式：恢复本次前端和文档变更并重建前端；无需清理 SQLite。

## 46. 2026-08-14 即梦式下拉与比例弹层细节修复

- 原因：图片模型菜单文字被截断，Select 内外出现重复边框，画面比例弹窗选中态过浅。
- 实现：Select 根节点改为无边框 36px 控件，工作流菜单最小宽度提升到 224px 并保留完整模型名称；比例设置 Popover 使用白色背景、深灰标题、蓝色边框和浅蓝选中背景。
- 兼容性：不改变工作流、参数和任务协议，只调整前端弹层和控件样式。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并在 Chrome 中检查模型下拉与比例 Popover 的 DOM/computed style。
- 回滚方式：恢复本次前端和文档变更并重建前端；无需清理 SQLite。
## 48. 2026-08-14 白色主题文字对比度与模型触发器修复

- 视频模型选择器加宽至完整展示默认的“MiniMax H3 首尾帧视频”，下拉菜单与触发器保持同一宽度，模型名称不再因默认状态而省略。
- 时长设置弹层的刻度、输入数值和滑轨改为白色背景下清晰可读的颜色；顶栏和其余白色主题文本不再被悬浮态白字规则误覆盖。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`；Chrome 中确认模型文字无横向溢出、时长刻度为深色文本。
- 回滚方式：恢复本次前端和文档变更并重新构建；不影响现有任务、SQLite 数据库、ComfyUI 节点或本地媒体。
## 49. 2026-08-14 创作工具栏控件防压缩修复

- 视频生成工具栏的媒体、模型、比例和时长控件不再被 flex 布局压缩；窄空间下会换行，保持控件边界和文字可用宽度。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`，并使用 Chrome DOM 检查工具栏的宽度与溢出状态。
- 回滚方式：恢复本次前端样式和文档变更并重建前端。
## 50. 2026-08-14 媒体类型下拉菜单宽度修复

- 图片生成/视频生成下拉菜单现在与 140px 触发器等宽，打开菜单时不再显得被压缩；视频模型控件继续完整显示。
- 验证：Chrome DOM 中媒体触发器和菜单均为 140px，模型控件为 248px；前端构建和 59 项后端测试通过。
- 回滚方式：恢复本次前端和文档变更并重建前端。

## 51. 2026-08-15 图片联合参数选择器

- 原因：图片生成工具栏在比例/分辨率联合控件之后仍显示独立的分辨率与数量控件，浅色画布上的上传图标也会因白色前景而不可见。
- 实现：注册表以 `ui_companions` 声明比例控件嵌入的分辨率与生成数量；前端按该声明只呈现一个摘要触发器和一个三组选择面板，上传入口改为蓝色图标和浅蓝底。
- 兼容性：`GET /api/modes` 仅新增可忽略的展示元数据；任务 options、SQLite、端口、ComfyUI 图和节点 ID 不变。
- 验证：`pnpm --dir frontend build`、`<ComfyUI Python> -m unittest backend.tests.test_ai_studio`，以及在桌面和 `390x844` Chrome 视口检查联合面板和上传图标。
- 回滚方式：恢复本次注册表、前端、测试和文档变更并重建前端；无需清理任务或媒体。

## 52. 2026-08-17 即梦式生成与资产任务栏

- 原因：左侧导航需要收敛为“生成”和“资产”两个工作区，并按即梦方式区分新对话、当前创作和最近任务；长任务列表需要可收起。
- 实现：前端新增生成/资产视图状态、当前创作与最近任务分组、固定全局导航和可折叠任务栏；资产页汇总生成结果，并兼容历史任务顶层输出记录。视频资产返回生成页时会恢复对应的视频工作流。
- 受影响文件：`frontend/src/App.tsx`、`frontend/src/index.css`、`README.md`、`docs/ARCHITECTURE.md`。
- 兼容性：不改变工作流注册表、任务 API、SQLite 字段、媒体路径、ComfyUI 节点或固定端口；移动端继续使用现有任务列表入口。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`；在 Chrome 桌面视口检查任务分组、收起和资产跳转，并在 `390x844` 视口检查无横向溢出。
- 回滚方式：恢复上述前端与文档变更并重建前端；无需迁移或清理任务、媒体或数据库。

## 53. 2026-08-17 即梦式资产媒体库

- 原因：资产页需要与即梦的生成历史一致，优先按日期浏览媒体，而不是显示带标题的方形任务卡。
- 实现：资产页提供“生成历史、主体、画布”标签、媒体类型筛选、搜索和排序入口；历史输出按任务创建日期分组，以 16:9 缩略图展示，视频显示时长角标。资产视图仅保留全局导航，任务栏完全隐藏而不再留下裁剪残影。
- 受影响文件：`frontend/src/App.tsx`、`frontend/src/index.css`、`README.md`、`docs/ARCHITECTURE.md`。
- 兼容性：筛选和标签仅作用于前端视图；不改变任务 API、SQLite、工作流注册表、ComfyUI 节点、媒体路径或端口。
- 验证命令：`pnpm --dir frontend build`、`python -m unittest discover -s backend/tests -p "test*.py"`；Chrome 桌面检查日期分组与五列网格，`390x844` 视口检查无横向溢出。
- 回滚方式：恢复上述前端和文档变更并重建前端；无需数据迁移或媒体清理。

## 54. 2026-08-17 LLM 大模型服务与提示词智能优化

- 原因：工作台创作者需要将简短创意快速扩写为富有运镜、光影与氛围的专业提示词；对接 ModelScope（魔搭社区）每日免费额度模型及通用 OpenAI 兼容协议。
- 实现：
  1. 后端新增通用 `OpenAICompatibleClient` 与 `LlmProviderService`，支持标准 `/chat/completions` 请求；
  2. SQLite 增加 `llm_provider_settings` 表，Key 经 Fernet 加密存储；默认预设 ModelScope 免费模型 `Qwen/Qwen2.5-72B-Instruct`；
  3. 提供管理员配置与连通测试接口 `/api/admin/providers/llm` 及提示词优化接口 `/api/llm/optimize-prompt`；
  4. 前端超级管理员设置提供“LLM 大模型”配置页（包含一键预设与测试按钮），工作台输入框提供一键“AI 优化”按钮与 Loading 态反馈。
- 受影响文件：`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/app/models.py`、`backend/app/storage.py`、`backend/app/main.py`、`backend/tests/test_llm.py`、`frontend/src/admin/LlmProviderSettings.tsx`、`frontend/src/Root.tsx`、`frontend/src/App.tsx`、`docs/ARCHITECTURE.md`、`README.md`、`功能说明与扩展指南.md`。
- 兼容性：新增独立的 Provider 表和 API 接口，完全向下兼容已有的任务数据、ComfyUI 实例与 GRS 服务；未启用 LLM 时工作台提示词输入保持原有手动编辑行为。
- 验证命令：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述代码与文档文件，重新执行前端 build 即可；数据库表无破坏性改动。

## 55. 2026-08-17 管理设置白色系高对比度视觉重构与本地凭据回退

- 原因：管理设置界面此前为暗黑背景，与 Ant Design 默认的浅色 Alert 警告框和输入控件搭配时文字对比度严重不足看不清；本地开发直接启动时未加载 `credential.key` 会出现“凭证主密钥不可用”提示。
- 实现：
  1. `backend/app/config.py` 中的 `credential_key` 增加自动本地回退读取 `data/credential.key`，免去手动配置环境变量；
  2. `AdminUsers`、`LlmProviderSettings`、`GrsProviderSettings`、`QiniuStorageSettings` 全部重构为白色系高对比度卡片布局（`#f8f9fa` 底色、`#ffffff` 卡片容器、`#111827` 标题与深色标签）；
  3. 在 `AGENTS.md` 中新增「白色系高对比度视觉标准」规则，规范后续后台与表单开发。
- 受影响文件：`backend/app/config.py`、`frontend/src/Root.tsx`、`frontend/src/admin/LlmProviderSettings.tsx`、`frontend/src/admin/GrsProviderSettings.tsx`、`frontend/src/admin/QiniuStorageSettings.tsx`、`AGENTS.md`、`docs/ARCHITECTURE.md`、`README.md`、`功能说明与扩展指南.md`。
- 兼容性：不改变 API、数据库和任务系统。
- 验证命令：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述前端与配置文件并重新构建前端。

## 56. 2026-08-17 MiniMax H3 官方提示词技能体系与通用大模型深度融合

- 原因：将 MiniMax-H3 官方开源提示词技能（涵盖全模态时序结构及 8 大细分风格技能）与工作台通用 LLM 优化器结合，实现符合工业级标准的电影运镜、环境音效与背景配乐结构化扩写。
- 实现：
  1. 后端建立 `backend/app/llm_minimax_skills.py` 技能知识库，涵盖通用电影级、极简电商广告、3D动画、纸艺定格、品牌宣传、音乐短片、双人游戏片头、纸拼贴与手绘实景混合；
  2. 根据参考图数量自动匹配 T2VA（0图）、I2VA（1图首帧对齐指令）与 Ref2VA（多图 `<Picture 1>` ~ `<Picture N>` 引用）；
  3. 新增 `GET /api/llm/skills` 与扩展 `POST /api/llm/optimize-prompt` 接口；
  4. 前端输入栏使用 Ant Design `Dropdown` 升级优化按钮，支持一键优化与 9 款 MiniMax 风格技能切换。
- 受影响文件：`backend/app/llm_minimax_skills.py`、`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/app/models.py`、`backend/app/main.py`、`backend/tests/test_llm.py`、`frontend/src/App.tsx`、`docs/ARCHITECTURE.md`、`README.md`、`功能说明与扩展指南.md`。
- 兼容性：完全兼容原有任务与模型，未选技能时默认电影级通用增强。
- 验证命令：`pytest backend/tests/test_llm.py`、`npm run build`。
- 回滚方式：恢复上述代码与文档，重新构建前端。

## 57. 2026-08-21 MiniMax H3 本地输出尺寸预设

- 原因：本地 H3 原有 `1K/2K/4K` 只是内部像素面积档位的错误文案，员工选择“2K”后会收到 `736×416` 等低于预期的文件。
- 实现：普通页面以实际输出尺寸作为主选择信息，并将 MP 放在次要说明。标准 H3 直接采用 MiniMax `ResolutionSelector` 的 `MP×1024²`、32 像素对齐算法，注册表提供 `.2` 至 `2.0 MP` 档位，16:9 实际最高为 `1920×1088`；移除了工作台自定义的 `1344×768` 画布限制。T8 全能参考和双时钟则遵守其自定义节点明确的像素面积限制，仍只提供 `.2` 至 `.98 MP`。前端从 `/api/modes` 动态读取工作流档位，在比例切换时即时重算尺寸。
- 兼容性：标准 H3 历史明确传入的 `1K/2K/4K` 请求仍保留 `0.2/0.3/0.5 MP` 映射；历史任务已经保存的 MP 不修改。T8 的最高 `0.98 MP` 与现有自定义节点的 `768×1344` 面积限制一致。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`；桌面与 `390×844` 检查标准 H3、全能参考、双时钟的尺寸档位和比例切换后的动态尺寸。
- 回滚方式：恢复 `workflow_registry.py`、测试及三份文档，重新构建前端；无需迁移数据库或删除任务、媒体。

## 58. 2026-08-24 局域网 IP 访问与七牛云直链交付

- 原因：局域网 IP 的 HTTP 页面不属于浏览器目录授权所需的安全上下文，强制弹出本地目录选择会让员工无法进入工作台。
- 当前基线：前端识别非回环 IP 地址访问时跳过本地目录初始化、强制弹窗和提交拦截；`127.0.0.1`、HTTPS/桌面客户端的既有本地目录交付不变。启用七牛云后新生成的输出会标记为 `cloud`，任务和作品库 API 对这类输出的 `download_url` 直接返回五分钟七牛云私有签名 URL，视频可直接播放或下载。
- 受影响文件：`frontend/src/App.tsx`、`backend/app/main.py`、`backend/app/models.py`、`backend/app/comfy_service.py`、`backend/app/worker.py`、`backend/tests/test_core.py`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：SQLite、任务队列、ComfyUI 节点/端口与已有下载端点不变；未启用七牛云时，本机安全上下文仍需要已授权目录，局域网 IP 可继续生成和查看服务器可用的结果。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`；分别以 `127.0.0.1`、局域网 IPv4 和启用七牛云的账号确认目录弹窗与视频直链行为。
- 回滚：恢复上述文件后重新构建前端并重启工作台；无需迁移或删除七牛云对象。

## 59. 2026-08-24 七牛云上传断连重试与大文件分片

- 原因：ComfyUI 已完成的视频在上传七牛云时可能因远端主动断开连接而被错误显示为“生成失败”。
- 实现：`QiniuStorage` 对超过 8 MiB 的视频改用七牛 v2 分片上传（`qiniu==7.16.0` 使用 `put_stream(..., version="v2")`，新版 SDK 使用 `put_stream_v2`）；对 `RemoteDisconnected`、连接重置、超时、408/429/5xx 及 SDK `status_code=-1` 进行最多 3 次指数退避重试，保留同一对象键避免重复产生孤儿对象。
- 受影响文件：`backend/app/qiniu_storage.py`、`backend/tests/test_ai_studio.py` 及三份项目文档。
- 兼容性：不改变工作流、任务接口、SQLite、ComfyUI 节点、固定端口或未启用七牛云时的本地交付行为。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复本次后端、测试和文档变更并重启工作台；无需删除云端媒体。

## 60. 2026-08-25 MiniMax H3 Web AI 导演台（Director Studio）系统

- 原因：吸收开源社区 Web 导演台与分镜系统（`NickPittas/DirectorsConsole`、`seesee75-Director`、`oh-my-minimaxh3-director` 与 `AIMixer`）的最佳实践，在工作台中原生提供一站式影视级分镜编排、运镜调度、角色一致性绑定、镜头连续性接龙与成片串播系统。
- 实现：
  1. 前端在左侧全局导航新增 **【🎬 导演台】**，与“生成”和“资产”并列，采用统一的白色系高对比度（Light Theme）视觉标准；
  2. `frontend/src/director/` 模块提供分镜故事板、机位与运镜（`CameraControlModal`）、AI 剧本拆解（`ScriptSplitModal`）、成片串播（`SequencePlayerModal`）及主体参考槽（`<Picture 1>`~`<Picture 9>`）；
  3. 后端新增 `POST /api/llm/split-script` 接口，结合内置大模型将自然语言剧本按电影工业视听语言精准拆解为结构化分镜头脚本；
  4. 分镜任务完全复用固定 ComfyUI 8188 实例的 MiniMax H3 动态 Graph（T2V/I2V/R2V）、任务排队机制与本地目录自动交付。
- 受影响文件：`backend/app/models.py`、`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/app/main.py`、`backend/tests/test_director.py`、`frontend/src/director/*`、`frontend/src/App.tsx`、`docs/ARCHITECTURE.md`、`README.md`、`功能说明与扩展指南.md`。
- 兼容性：完全向下兼容，不破坏原有任务、数据库结构、ComfyUI 实例、节点 ID 或工作流协议；导演工程在前端持久化管理并与后端任务引擎无缝同步。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述代码和文档后重新构建前端；无需数据库迁移。

## 61. 2026-08-25 云端媒体下载改为同源代理流

- 原因：七牛云启用后下载接口返回 HTTP 307，Toonflow 等客户端不跟随重定向，会报 `Toonflow video download failed: HTTP 307`。
- 当前基线：任务里的 `download_url` 仍是稳定的同源下载路径；后端自行请求五分钟私有签名链接并把视频/图片字节流式转发给客户端，状态码为 200（支持 Range 时为 206），不再对外 307。
- 受影响文件：`backend/app/main.py`、`backend/tests/test_core.py`、`docs/ARCHITECTURE.md`、`README.md` 和本文档。
- 兼容性：不改变任务接口字段、SQLite、ComfyUI 节点、固定端口或未启用七牛云时的本地/流式交付。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述文件并重启工作台；无需迁移或删除云端对象。

## 62. 2026-08-25 ComfyUI 重启后自动接回或重提中断视频任务

- 原因：本机 ComfyUI 不能保证一直不关机。卡死重启后，ComfyUI 内存队列和历史会丢，工作台里进行中的视频任务此前只能停在“已中断”，必须手动重新提交。
- 实现：工作台 worker 持续侦听固定 ComfyUI。若原任务还在 `/queue` 或 `/history` 中，则接回并继续同步；若 ComfyUI 已起来但原任务不存在，则用 SQLite 里保存的提示词和参数自动重新提交，最多 3 次。ComfyUI 连不上时不重提，以免重复占卡。未完成的那一段显存推理无法续跑，等于从这条任务从头再生成。任务详情仍保留“重新提交”。
- 受影响文件：`backend/app/worker.py`、`backend/app/comfy_service.py`、`backend/tests/test_core.py`、`frontend/src/App.tsx`、`README.md`、`docs/ARCHITECTURE.md`、`docs/API.md` 和本文档。
- 兼容性：不改数据库结构、节点 ID、模型路径和端口。图片任务规则不变。
- 验证命令：`python -m unittest discover -s backend/tests -p test_core.py`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述文件并重启工作台；无需清理 SQLite 或 ComfyUI 输出。

## 62. 2026-08-25 GRS 生图模型可配置目录

- 原因：生图只写死 GPT Image 2 / VIP，无法使用 GRS 文档中的 Nano Banana 等模型。
- 实现：新增 `grs_image_models` 目录表与内置种子；管理后台以表格启用/改名/设默认/添加自定义模型，并可同步内置目录。`GET /api/modes` 为每个启用模型生成独立图片工作流，参数 schema 由能力档生成，`provider_model` 作为内部字段提交给 `/v1/api/generate`。
- 受影响文件：`backend/app/grs_catalog.py`、`backend/app/workflow_registry.py`、`backend/app/storage.py`、`backend/app/grs_provider.py`、`backend/app/models.py`、`backend/app/main.py`、`backend/app/worker.py`、`frontend/src/admin/GrsProviderSettings.tsx`、测试与三份主文档。
- 兼容性：历史任务 mode 与 GRS 提交协议不变；旧开关和逗号分隔模型列表在首次启动时迁入目录。默认仍只启用 GPT Image 2 / VIP。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台；无需删除任务或媒体。

## 63. 2026-08-25 GRS 真人参考图失败时接回已出图并展示审核原因

- 原因：参考图含真人时 GRS 常返回违规；少数图已写入员工目录，但查询 HTTP 400、成功后回写失败、失败任务隐藏下载地址，导致界面一律显示生成失败。
- 实现：结果查询解析 400 JSON 并保留 URL；成功暂无 URL 继续轮询；下载按 URL 列表尝试；落盘后禁止改回失败；有输出的轮次至少为部分完成；任务详情展示审核/失败原因和已有图片。
- 受影响文件：`backend/app/grs_client.py`、`backend/app/worker.py`、`backend/app/storage.py`、`backend/app/main.py`、`frontend/src/App.tsx`、`frontend/src/index.css`、测试与三份主文档。
- 兼容性：不改数据库结构、ComfyUI 节点、模型路径和端口。历史「失败但有输出」任务刷新后变为部分完成。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述文件并重启工作台。

## 64. 2026-08-25 导演台编译器对齐现有 H3 T2V/I2V/R2V

- 原因：导演台提交未使用编译提示词，参考图 `<Picture n>` 与 multipart 顺序错位，批量渲染未等成功即入队，Analyze 只发文字假装看图。
- 实现：
  1. 抽出 `prompt-compiler.ts` / `director_compiler.py`：锁定 Picture 编号、2–15 秒吸附、≤15s 整段 vs 单镜；
  2. `renderShot` 提交编译结果，按 plan 顺序挂 `references`，按 T2V/I2V/R2V 路由，options 走注册表画质 1K/2K/4K；
  3. 批量渲染改为成功后抽尾帧再交下一镜；检视器显示即将提交的提示词；
  4. Analyze 有视觉则带图，否则禁用；导演台接入已有剪映导出；localStorage 保存前剥离 `File`，仅保留 `data:` 预览。
- 受影响文件：`frontend/src/director/*`、`backend/app/director_compiler.py`、`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/app/main.py`、`backend/app/models.py`、`backend/tests/test_director.py`、`docs/ARCHITECTURE.md`、`docs/API.md`、`README.md` 和本文档。
- 兼容性：不改 SQLite、ComfyUI 节点 ID、固定 8188 端口或 `/api/jobs` 协议。
- 验证命令：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚方式：恢复上述文件并重新构建前端；无需数据库迁移。

## 65. 2026-08-25 Docker 构建先升级 pip 并钉死 pydantic-core

- 原因：Docker 运行阶段使用镜像自带 pip 24.0 安装 `pydantic==2.13.4` 时，若 `pydantic-core==2.46.4` 索引拉取失败，构建会以 `from versions: none` 退出。
- 实现：Dockerfile 先升级 pip，再按 `backend/requirements.txt` 安装二进制轮子，并显式钉死 `pydantic-core==2.46.4`。构建默认走清华 PyPI 与 npmmirror，可用 `PIP_INDEX_URL`、`NPM_CONFIG_REGISTRY` 覆盖。
- 受影响文件：`Dockerfile`、`compose.yaml`、`.env.example`、`backend/requirements.txt`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：不改工作台端口、SQLite、ComfyUI 实例或 API。
- 验证命令：`docker compose build`。
- 回滚方式：恢复上述文件后重新构建镜像。

## 66. 2026-08-25 Ollama 本地连接测试等待模型加载

- 原因：Ollama 进程与模型都正常时，管理后台测试仍用 15 秒对话超时。7B 首次装入显存经常超时，被显示成连接失败。
- 实现：本地 Base URL 测试等待 90 秒；先查 `/v1/models`，名称不一致时列出已安装模型。前端 Ollama 预设补充 `qwen2.5:7b-instruct`，并说明 Token 可留空。
- 受影响文件：`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/tests/test_llm.py`、`frontend/src/admin/LlmProviderSettings.tsx` 与三份主文档。
- 兼容性：云端 LLM 测试仍为 15 秒；不改数据库、端口或 ComfyUI。
- 验证：`python -m unittest backend.tests.test_llm`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 67. 2026-08-25 魔搭 LLM 按魔粒计费说明

- 原因：工作台把魔搭标成「免费额度」，实际调用会扣除账户魔粒（含签到/任务/充值余额）；DeepSeek-V4 默认开思考会进一步放大消耗。
- 实现：设置页改为「按魔粒计费」并警告无法只花每日赠送；推荐硅基流动免费 7B 或本机 Ollama。请求 DeepSeek-V4 时发送 `thinking.type=disabled`。
- 受影响文件：`backend/app/llm_client.py`、`backend/tests/test_llm.py`、`frontend/src/admin/LlmProviderSettings.tsx`、`docs/ARCHITECTURE.md`、`README.md` 和本文档。
- 兼容性：不改数据库、端口或 ComfyUI；已保存的魔搭配置仍可用，只是不再被宣传为免费。
- 验证：`python -m unittest backend.tests.test_llm`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 68. 2026-08-25 LLM 后台拉取官方免费模型

- 原因：设置页只有几个推荐模型标签，无法从硅基流动官方目录选择当前 Free 模型。
- 实现：新增 `POST /api/admin/providers/llm/models`，拉取上游完整 `/v1/models`；硅基流动用名称或标记中的 Free 文字筛选。前端改为可搜索下拉框。
- 受影响文件：`backend/app/llm_client.py`、`backend/app/llm_provider.py`、`backend/app/models.py`、`backend/app/main.py`、`backend/tests/test_llm.py`、`frontend/src/admin/LlmProviderSettings.tsx` 与三份主文档。
- 兼容性：不改数据库、端口或 ComfyUI。
- 验证：`python -m unittest backend.tests.test_llm`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 69. 2026-08-25 硅基流动免费模型对照模型广场价格

- 原因：`/v1/models` 是 OpenAI 兼容格式，没有 Free 文字，后台筛选结果为空。
- 实现：继续拉取账号可调用目录；硅基流动再读取公开模型广场，按价格为 0 / Free 徽章标记免费模型，并与可调用 ID 求交。下拉框在已选中某一项时仍展示全部已加载模型；OCR / 嵌入 / 语音等非对话模型不进入 LLM 目录。
- 受影响文件：`backend/app/llm_client.py`、`backend/tests/test_llm.py`、`frontend/src/admin/LlmProviderSettings.tsx` 与三份主文档。
- 兼容性：不改数据库、端口或 ComfyUI。
- 验证：`python -m unittest backend.tests.test_llm`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 70. 2026-08-25 导演台融合版界面冻结与实现

- 原因：高保真设计确认后，导演台需要一个稳定的镜头导航—预览—检视器—时间轴工作区，并在移动端保持核心生成与连续性操作可达。
- 实现：`DirectorStudioModule` 采用融合版布局；右侧内联展示首尾帧槽、主体引用、提示词、机位标签和编译预览；完整提示词与运行参数默认折叠。移动端新增工作区导航、横向镜头序列、底部固定操作栏；`ScriptSplitModal` 改为纯产品语义中文。
- 受影响文件：`frontend/src/App.tsx`、`frontend/src/index.css`、`frontend/src/director/DirectorStudioModule.tsx`、`frontend/src/director/components/ScriptSplitModal.tsx`。
- 兼容性：保留 H3 T2V / I2V / R2V 路由、串行接龙、主体参考顺序和 `<Picture n>` 编译；不安装第三方导演节点、不改节点 ID、端口、数据库或任务 API。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`；浏览器回归视口为 1440×900 与 390×844。
- 回滚：恢复上述前端文件并重新构建；localStorage 工程与历史任务无需处理。

## 71. 2026-08-26 工作台手动停止生成

- 原因：只在 ComfyUI 里删执行中任务会被工作台当成丢失并自动重提；用户需要工作台上的「停止生成」，停止后不再自动重跑。
- 实现：新增 `POST /api/jobs/{job_id}/cancel` 与任务状态 `cancelled`。视频向固定 ComfyUI 8188 中断对应正在运行的 prompt 或删除排队项；worker 恢复逻辑跳过已停止任务。图片只停本地等待。创作台任务详情/列表和导演台均可停止，之后可手动重新提交。
- 受影响文件：`backend/app/models.py`、`backend/app/storage.py`、`backend/app/comfy_service.py`、`backend/app/worker.py`、`backend/app/main.py`、测试、`frontend/src/App.tsx`、`frontend/src/director/*` 与三份主文档。
- 兼容性：SQLite 自动增加 `cancel_requested` 列；不改 ComfyUI 节点、端口或创建任务接口。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 72. 2026-08-26 视频时长下限改为 2 秒

- 原因：全能参考 / 双时钟工作流滑杆已能选 2 秒，但标准 H3 校验仍要求 5–15 秒，导演台会把 2 秒静默改成 5 秒，用户传入的短时长不进入 ComfyUI graph。
- 实现：注册表 `duration` 统一为 2–15 秒；`h3_length()` 把 2 秒映射为 56 帧（24fps、17n+5 网格）；导演台吸附下限改为 2 秒。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/director_compiler.py`、`frontend/src/director/prompt-compiler.ts`、导演台时长输入、测试与三份主文档。
- 兼容性：不改节点 ID、端口、数据库或创建任务接口；默认时长仍为 5 秒（T8 全能参考默认 8 秒）。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 73. 2026-08-26 导演台生成状态左右对齐

- 原因：左侧镜头按 `queued` 显示「排队中」，中央预览把排队/运行都写成「生成中」；创建任务返回入队旧快照，导演台只靠任务列表同步，ComfyUI 已开始执行时左侧仍停在排队。
- 实现：`POST /api/jobs` 与下一轮接口返回入队后的当前快照；导演台用任务状态映射函数统一文案，提交后立即按单任务接口轮询，不把已经 `running` 的镜头回退成排队。
- 受影响文件：`backend/app/main.py`、`backend/tests/test_director.py`、`frontend/src/director/*` 与三份主文档。
- 兼容性：不改节点 ID、端口、数据库或创建任务字段。
- 验证：`python -m unittest backend.tests.test_director`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 74. 2026-08-26 视频生成速度预设与自定义步数

- 原因：标准 H3 固定 20 步偏慢；全能参考 / 双时钟虽已挂加速 LoRA，但步数不能在创建页选择。
- 实现：注册表新增 `speed`（快速 4 步 / 均衡 8 步 / 高质量 20 步 / 自定义）和 `custom_steps`（1–40，仅自定义时显示）。4–8 步加载 Turbo LoRA；超过 8 步关闭加速。全能参考将视频与音频采样都映射为同一自定义步数。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/minimax_h3_workflow.py`、`backend/tests/test_core.py`、`frontend/src/App.tsx` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库；默认均衡 8 步。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 75. 2026-08-26 生成页管理员切换用户查看任务

- 原因：资产页已有用户筛选，生成页任务栏没有入口，管理员无法在创作区查看指定员工的生成任务。
- 实现：生成页桌面任务栏和移动端任务列表增加与资产页共用的用户下拉。`GET /api/jobs?user_id=` 仅管理员有效；切到具体他人时隐藏创作表单、新对话和继续生成操作，仍可查看详情、预览和下载。新建任务仍归属当前管理员。
- 受影响文件：`frontend/src/App.tsx`、`frontend/src/media/ImageStudioModule.tsx`、`frontend/src/index.css`、`backend/tests/test_ai_studio.py`、`backend/app/api_documentation.py` 与三份主文档。
- 兼容性：不改节点 ID、端口、数据库或任务归属写入规则。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`；浏览器检查桌面与约 390px 视口的用户切换与只读查看。
- 回滚：恢复上述文件并重新构建前端。

## 76. 2026-08-26 全能参考参考图改走 Ref2VA

- 原因：全能参考有图时仍加载 FL2VA，参考图连上了但权重不吃参考条件；H3 提示词还把 `@图片1` 误替换成旧 VACE 文案。
- 实现：T8 全能参考无图保持 FL2VA/`T2VA`；有图改为 Ref2VA 权重与 `Ref2VA` 任务类型，LoRA 加载器跟随实际 UNet。H3/T8 将 `@图片n`/`@图n` 转为 `<Picture n>`，未写标签时自动补引用。
- 受影响文件：`backend/app/minimax_h3_t8_workflow.py`、`backend/app/comfy_service.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 77. 2026-08-26 双时钟单图改为 I2VA 首帧

- 原因：双时钟加速上传 1 张参考图后按 Ref2VA autogrow 提交，ComfyUI T8 条件节点报 `name 'task_type' is not defined`；该路径也无法把 Turbo LoRA 用在 pruned Ref2VA 上。
- 实现：`task_type` 始终先解析再写入 graph。双时钟 `auto`：无图 `T2VA`，单图 `I2VA` 并连接 `first_frame`，保持 FL2VA 与 Bypass LoRA。全能参考有图仍走 Ref2VA。
- 受影响文件：`backend/app/minimax_h3_t8_workflow.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库；显式 `task_type=Ref2VA` 仍按参考图连接。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 78. 2026-08-26 pruned Ref2VA 不再加载 Turbo LoRA

- 原因：全能参考有图后加载 pruned Ref2VA 仍挂 Turbo LoRA。LoRA 的 AdaLN 为 2688 维，pruned 权重为 8 维，ComfyUI 在 Model Initializing 阶段报 `adaIn_proj.linear.weight` 形状无效。
- 实现：`h3_turbo_lora_compatible()` 拒绝文件名含 `pruned` 的 UNet。T8 全能参考有图与标准 H3 R2V 跳过 LoRA 节点，仍按所选速度步数采样；无图 T8 / 文生 / 首尾帧继续对 FL2VA 全量权重使用 Bypass LoRA。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/minimax_h3_t8_workflow.py`、`backend/app/minimax_h3_workflow.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库。速度预设仍写入 `lora_strength`，graph 构建时对 pruned 权重强制不挂 LoRA。
- 验证：`python -m unittest discover -s backend/tests -p "test_*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 79. 2026-08-26 导演台项目库与 SQLite 工程文档

- 原因：导演台把时间轴当首页，工程切换入口不可见，剧本原文不落盘。
- 实现：新增 `director_projects` 表与 CRUD/迁库 API，员工只能看到自己的工程。前端默认进项目库，空白工程不再预置 3 条演示分镜；示例改为空状态「用示例创建」。时间轴防抖 PUT；拆分结果连同 `source_script` 写入工程。
- 受影响文件：`backend/app/storage.py`、`backend/app/models.py`、`backend/app/main.py`、`backend/app/api_documentation.py`、`backend/tests/test_director.py`、`frontend/src/director/*`、`frontend/src/index.css` 与三份主文档。
- 兼容性：不改节点 ID、端口或 `POST /api/jobs`。首次打开迁入 localStorage，迁完打标。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重新构建前端、重启工作台；新表可保留。

## 80. 2026-08-26 导演台剧本文档与拆分确认

- 原因：拆分会覆盖当前分镜，原文关闭弹窗后不易回看；移动端旧「切换工程」曾绑到改标题。
- 实现：项目库从剧本创建会新建工程并写入 `source_script`。已打开工程再拆需确认替换或另存，有已生成 Take 时默认另存。工作区抽屉可回看和手改原文，空文案也可保存。移动端返回项目库，标题不再作为切换入口。
- 受影响文件：`frontend/src/director/DirectorStudioModule.tsx`、`frontend/src/director/components/ScriptDocumentDrawer.tsx`、`frontend/src/director/components/ScriptSplitModal.tsx`、`frontend/src/director/types.ts`、`frontend/src/index.css`、`backend/tests/test_director.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或 `POST /api/jobs`。
- 验证：`python -m unittest discover -s backend/tests -p "test*.py"`、`pnpm --dir frontend build`；浏览器核对应项目库、从剧本/手写原文、刷新后工程和原文仍在、桌面与移动端返回项目库。
- 回滚：恢复上述文件并重新构建前端。

## 81. 2026-08-26 多参考加速改用全量 Ref2VA INT8

- 原因：本机已下载非 pruned Ref2VA；有图任务仍强制 pruned，Turbo LoRA 被跳过。
- 实现：快速/均衡的 R2V 与 T8 全能参考有图改为 `minimax_h3_ref2va_int8_convrot.safetensors` + Bypass LoRA。高质量仍用 pruned。不引入 0.4→2.0 二采。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/minimax_h3_workflow.py`、`backend/app/minimax_h3_t8_workflow.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`。
- 回滚：恢复上述文件并重启工作台。

## 82. 2026-08-26 导演台预览/成片两档渲染

- 原因：导演台需要低成本打样；旧 `1K/2K/4K` 实际落到 0.2/0.3/0.5 MP。
- 实现：预览固定 0.4 MP + 4 步 + LoRA。成片默认 1.0 MP + 8 步 + LoRA，顶栏可选 0.4/1.0/2.0。批量接龙与整段走成片。Take 标记 `renderPass`。
- 受影响文件：`frontend/src/director/*`、`frontend/src/index.css`、`backend/app/director_compiler.py`、`backend/tests/test_director.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或 `POST /api/jobs` 字段。
- 验证：`python -m unittest backend.tests.test_director`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重新构建前端。

## 83. 2026-08-26 导演台预览/成片步数与 MP 可调

- 原因：预览 4 步在标准 H3 上容易彩斑；成片步数此前写死，用户无法按工程改 MP 和步数。
- 实现：工程 payload 增加 `previewQuality` / `previewSpeed` / `finalQuality` / `finalSpeed`。设置条用 Ant Design Select。默认仍是预览 0.4 MP + 4 步、成片 1.0 MP + 8 步。旧工程无新字段时成片 MP 跟 `canvasTier`。
- 受影响文件：`frontend/src/director/*`、`frontend/src/index.css`、`backend/app/director_compiler.py`、`backend/tests/test_director.py` 与三份主文档。
- 兼容性：不改节点 ID、端口或 `POST /api/jobs` 字段。
- 验证：`python -m unittest backend.tests.test_director`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重新构建前端。

## 84. 2026-08-27 未选自定义时不回显自定义步数

- 原因：创建页已隐藏未选中的自定义步数，任务详情仍把默认 8 步列出来，和实际采样步数冲突。
- 实现：`request_parameters` 遵守 `ui_visible_when`；快速/均衡/高质量不回显 `custom_steps`，仅自定义回显。VIP 自定义宽高同样只在 CUSTOM 时出现。创建提交不再带上隐藏字段。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/main.py`、`backend/tests/test_core.py`、`frontend/src/App.tsx` 与三份主文档。
- 兼容性：不改节点 ID、端口或数据库；已有任务刷新详情即可。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 85. 2026-08-27 本机 Windows 后端热更新与可关闭控制台

- 原因：Windows 上 `uvicorn --reload` 的 StatReload 在保存代码后把整个控制台进程组一起结束，服务表现为挂掉且不能热更新；关闭 CMD 时又卡在 `Waiting for application shutdown`，窗口关不掉。
- 实现：新增 `backend/dev_reloader.py`，启动脚本直接运行该文件（不用 `-m`，以兼容整合包 `python310._pth`），以独立进程组、不接收控制台关闭事件的方式拉起 uvicorn（不再使用 `--reload`）。监视 `backend/app` 源码变更后强制结束子进程树并重启；崩溃退避重启。启动时关闭 CMD QuickEdit，避免鼠标点选暂停进程。点击关闭窗口或 Ctrl+C 立即 `taskkill /F /T`。worker 关闭增加超时。7865 上已有本工作台但不响应健康检查时，启动脚本先结束再启动。Docker 生产路径不变。
- 受影响文件：`backend/dev_reloader.py`、`backend/app/worker.py`、`backend/tests/test_dev_reloader.py`、`backend/requirements.txt`、`启动本地视频工作台.bat`、`README.md`、`docs/ARCHITECTURE.md` 和本文档。
- 兼容性：不改 ComfyUI 节点、端口、SQLite 或任务协议。
- 验证命令：`python -m unittest discover -s backend/tests -p "test_dev_reloader.py"`、`python -m unittest discover -s backend/tests -p "test*.py"`。
- 回滚方式：恢复上述文件，启动脚本改回 `uvicorn --reload --reload-dir backend`。

## 86. 2026-08-27 LightX2V 工作流接入与创作页分组

- 原因：本机 LightX2V 加速 LoRA 需要独立工作流；创作页工作流增多后需要按家族分组。
- 实现：新增 LightX2V 文生 / 首尾帧 / 多参考三个动态 API graph（euler、1.0 MP、LightX2V LoRA）。官方三个 H3 与 T8 不改节点。`GET /api/modes` 增加分组字段；下拉分为 LightX2V、官方 MiniMax H3、自定义。ComfyUI `extra_model_paths.yaml` 增加 `loras: lightx2v`，模型仍留在 `G:\ComfyUI-Models\lightx2v`。
- 受影响文件：`backend/app/workflow_registry.py`、`backend/app/minimax_h3_lightx2v_workflow.py`、`backend/app/models.py`、`backend/app/comfy_service.py`、`backend/tests/test_core.py`、`frontend/src/App.tsx`、`frontend/src/index.css` 与三份主文档。
- 兼容性：不改已有节点 ID、端口或数据库；改 extra_model_paths 后需重启 ComfyUI。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台与 ComfyUI。

## 87. 2026-08-27 LightX2V JSON 写入 ComfyUI 工作流库

- 原因：ComfyUI 侧边栏只显示 `user/default/workflows` 下的前端 JSON，工作台 API graph 不会出现在那里。
- 实现：复制到 `Comfyui/user/default/workflows/LightX2V/`（文生、首尾帧、多参考加速），LoRA 路径改为 extra_model_paths 文件名。首尾帧由文生图接上首/尾帧 LoadImage。
- 兼容性：不改官方 MiniMax H3 JSON、节点或端口。侧栏刷新可见；LoRA 仍需重启 ComfyUI。
- 回滚：删除该 LightX2V 目录。

## 88. 2026-08-27 LightX2V ComfyUI JSON 对齐本机模型名

- 原因：三个侧栏 JSON 仍指向本机不存在的带前缀 / experimental / BF16 权重，以及没有的 `taeh3.safetensors`。
- 实现：加载器改为无前缀全量 INT8 FL2VA/Ref2VA、nvfp4 CLIP、本机 VAE；多参考 LoRA 改为 Ref2V 4 步；预览 tiny VAE 设为 `none`。
- 兼容性：不改官方 H3 JSON、节点或端口。需从侧栏重新打开工作流。
- 回滚：从 `G:\ComfyUI-Models\lightx2v\工作流` 重新复制源 JSON。

## 89. 2026-08-27 LightX2V 去掉本机没有的 RAMCleanup

- 原因：文生视频报缺失节点包 `Comfyui-Memory_Cleanup`。固定 ComfyUI 没有该包，也不安装新 custom_nodes。
- 实现：三份侧栏 JSON 删除 `RAMCleanup`；多参考同时删掉未启用的 RTX 超分链路。生成仍保存到原 `SaveVideo`。
- 兼容性：不改官方 H3 JSON、端口或工作台 API。需从侧栏重新打开。
- 回滚：从 `G:\ComfyUI-Models\lightx2v\工作流` 重新复制源 JSON。

## 90. 2026-08-27 安装缺失节点并恢复 LightX2V 原版 JSON

- 原因：补齐 `Comfyui-Memory_Cleanup` 与 NVIDIA RTX 节点，恢复源工作流。
- 实现：克隆两个 custom_nodes，安装 `nvidia-vfx`；侧栏三份 JSON 恢复源图结构（含 RAMCleanup / RTX），加载器使用本机无前缀模型名。
- 兼容性：不改官方 H3 JSON、端口或工作台 API。需重启 ComfyUI 以加载新节点。
- 回滚：删除两个 custom_nodes 目录并卸载 `nvidia-vfx`。

## 91. 2026-08-27 LightX2V 模型路径改回本机无前缀文件

- 原因：整份恢复源 JSON 后又带上别人机器的模型前缀，ComfyUI 再次报找不到模型。
- 实现：保留 RAMCleanup / RTX 节点；UNET/CLIP/VAE/LoRA/tiny VAE 改回本机文件名。
- 兼容性：不改官方 H3 JSON、端口或工作台 API。需从侧栏重新打开。
- 回滚：仅模型控件可再拷源 JSON，但会重新找不到模型。

## 92. 2026-08-27 视频任务空闲后释放 ComfyUI 显存与内存

- 原因：ComfyUI 默认缓存已加载模型；工作台官方 H3、LightX2V、T8 的 API graph 都没有清理节点，Worker 也不调用 `/free`，任务列表空闲时显存/内存仍接近满载。
- 实现：Worker 在启动空闲时、以及最后一条本地视频任务结束且 ComfyUI 队列为空时 `POST /free`，并提交仅含已安装 `VRAMCleanup` / `RAMCleanup` 的短 prompt 以立刻唤醒 prompt worker。`RAMCleanup` 只整理 ComfyUI 自身工作集，不扫描其他进程。队列中还有下一条视频时保持加载。不改生成图节点 ID，也不把清理节点塞进 H3/LightX2V/T8 API graph。
- 受影响文件：`backend/app/comfy_service.py`、`backend/app/worker.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：不改端口、SQLite 或 `POST /api/jobs`。下一条视频会重新装模。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`。
- 回滚：恢复上述文件并重启工作台。

## 93. 2026-08-27 八步双加速工作流分组

- 原因：参考 JSON「minimax_h3八部双加速」的加速链（8 步 FL2V Turbo LoRA + `PathchSageAttentionKJ` + H3 Mem Eff Sage）需要独立分组；不能把 Director 前端节点提交为 API graph。
- 实现：新增 `minimax-h3-dual-accel-t2v` / `i2v` / `r2v`。加速链为全量 INT8 → 8 步 LoRA（强度 1.0）→ KJ Sage → H3 Sage → SigmaShift；默认 0.4 MP、8 步 `res_multistep`。高质量关 LoRA 用 pruned。官方 H3 / LightX2V / T8 节点 ID 不改；导演台仍走官方三个模式。
- 受影响文件：`backend/app/models.py`、`backend/app/workflow_registry.py`、`backend/app/minimax_h3_dual_accel_workflow.py`、`backend/app/comfy_service.py`、`backend/tests/test_core.py` 与三份主文档。
- 兼容性：新 mode 为增量 ID；LoRA 已在 `loras: lightx2v`。
- 验证：`python -m unittest discover -s backend/tests -p "test_core.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 94. 2026-08-27 ComfyUI 连接地址可配置

- 原因：ComfyUI 地址只能改环境变量并重启，管理后台看不到当前连接目标。
- 实现：新增 `comfy_provider_settings` 与 `/api/admin/providers/comfy`；超级管理员在 AI 供应商页保存/测试地址。`ComfyService` 运行时读取该地址。环境变量仅用于首次播种。浏览器直连交付仍固定本机 `8188/view`。
- 受影响文件：`backend/app/comfy_provider.py`、`comfy_service.py`、`storage.py`、`main.py`、`models.py`、前端 `ComfyProviderSettings.tsx`、`Root.tsx`、测试与三份主文档。
- 兼容性：不改节点、`7865` 或任务协议；已有库自动建表。
- 验证：`python -m unittest discover -s backend/tests -p "test_comfy_provider.py"`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重启工作台。

## 95. 2026-08-27 导演台画风目录与 Recipe payload

- 原因：导演台要对标 Recipe 方案（剧本/画风/人物/场景/分镜），现有时间轴 JSON 不够；画风必须来自固定目录。
- 实现：新增 `director_catalog/art_styles.json`（9 类 34 条，`promptPrefix` 复制 OpenDirector 种子）与 `GET /api/director/art-styles`。`payload.kind` 区分旧时间轴 / `director_recipe` / `batch_run`。Recipe 含 script、artStyle、characters、locations、scenes[].shots、agentStatus；artStyle 只能选自目录。`POST .../convert-to-recipe` 把时间轴 shots 映射为 scenes。不改 `director_projects` 表结构。
- 受影响文件：`backend/app/director_catalog/`、`director_recipe.py`、`storage.py`、`models.py`、`main.py`、`frontend/src/director/types.ts`、`director-api.ts`、`backend/tests/test_director.py` 与三份主文档。
- 兼容性：无 `kind` 的旧 payload 仍按时间轴读写；不自动转换。不改节点、端口、`POST /api/jobs`。
- 验证：`python -m unittest backend.tests.test_director`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重新构建前端、重启工作台。

## 96. 2026-08-27 导演台双引擎（对标 OpenDirector 产品形态）

- 原因：时间轴与底栏 9 槽不适合人物/场景一等公民和短视频批量；要对标对话导演 → Recipe → 出片，媒体仍用 GRS + 固定 8188 MiniMax H3。
- 实现：新增 `director_agents.py`（9 Agent 顺序调度）与 `director_jobs.py`（GRS 定妆、H3 分镜、批量 T2V）。新接口：`POST /api/director/recipes/run`、`/step`、`/generate-assets`、`/render-shots`、`POST /api/director/batches`。前端导演台改为双引擎首页、Recipe 编辑器、人物/场景卡、分镜卡片墙；去掉时间轴主操作面。提交时自动把角色/场景图编成 ≤9 张 `<Picture n>`。不引入 LangGraph / WaveSpeed / Pexels / Edge TTS。
- 受影响文件：`backend/app/director_agents.py`、`director_jobs.py`、`director_compiler.py`、`llm_provider.py`、`main.py`、`models.py`、`frontend/src/director/*`、`frontend/src/index.css`、测试与三份主文档。
- 兼容性：不改节点 ID、端口、`POST /api/jobs`；`POST /api/llm/split-script` 保留。旧时间轴打开时转为 Recipe。
- 验证：`python -m unittest backend.tests.test_director`、`pnpm --dir frontend build`。
- 回滚：恢复上述文件并重新构建前端、重启工作台。
